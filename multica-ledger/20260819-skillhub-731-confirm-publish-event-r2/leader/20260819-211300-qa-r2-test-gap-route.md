---
ts: 20260819-211300
agent: leader
action: qa r2 test gap route
tree: 71d9c55387b0a0b8336905d3aa9986fff6be6c97
verdict: RETURN
---

独立 QA R2 对业务 subject tree `71d9c55387b0a0b8336905d3aa9986fff6be6c97` 给出 `PLAN_COMPLIANCE=FAIL`、`SYSTEM_BEHAVIOR=FAIL`，主类为 `TEST_GAP`，未判定生产实现存在缺陷。该 FAIL supersede Leader 的 Implementation R6 Gate 对进入 Review 的任何放行含义；历史结论保留，不改写。

QA 已动态确认现有 PUT 授权矩阵、publish/yank mixed fan-out 子集、三类权威读取在 mocked dispatcher 前 fail closed、Dispatcher 单独的 persistence/SSE 参数，以及全 reactor 回归均通过。但冻结 R6 要求的同一生产事件端到端证据尚未闭合：现有 listener 测试终止于 mocked `NotificationDispatcher`，Dispatcher 测试又从手工 `dispatch(...)` 开始，不能证明同一 eligibility 结果沿真实 listener → Dispatcher → `NotificationService.create` → SSE 只作用于合格 recipient，也不能证明权威读取失败在最终 sink 上为 0 persistence/0 SSE。

按 finding 分类路由：由 Coder 在允许的测试写集中补真实最终 sink 探针与缺失 fan-out 授权矩阵；不得借此改生产行为。至少覆盖 publish/yank 的合格与不合格 mixed recipients、publisher/actor 排除、platform-only SUPER_ADMIN、hidden、cross-namespace、fallback/no-fallback，以及 account/namespace/membership batch failure 在最终 sink 上整批 0 persistence/0 SSE。若补探针在当前候选上 RED，则按实际失败分类；需要改生产代码时视为业务 tree 变化，旧 QA 全部失效并重走实现 Gate。

## 交接

- 状态：IMPLEMENTING（TEST_GAP 返修）
- 产物：QA R2 finding `qa/20260819-211005-regression-r2.md`
- 下一步：Coder 严格 TDD 补最终 sink 与缺失授权矩阵证据，然后重新独立 QA
- 未解决：真实 listener 到 persistence/SSE 的动态资格链未闭合；禁止进入 Review 或创建上游 PR
- 需要决策：无
