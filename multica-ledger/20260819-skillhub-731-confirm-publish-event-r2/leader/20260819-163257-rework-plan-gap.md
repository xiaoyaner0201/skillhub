---
ts: 20260819-163257
agent: leader
action: superseding finding / rework route
tree: 3cea02cf72013c9798fa8071e5429da13197fd66
verdict: RETURN
---

本分录追加记录 2026-08-19 的新 finding，保留但 supersede 旧 Plan、QA PASS、Reviewer PASS 与 Review Gate；这些旧结论仅是其原 subject tree 的历史证据，不再构成 publication authorization。

主分类为 `PLAN_GAP`，并关联 `INVARIANT_VIOLATION`：原 Plan 只证明 producer 发出事件，未覆盖新增可达的 subscriber consumer、当前接收者资格与通知持久化 sink。历史订阅不证明事件发生时仍具备 PRIVATE skill 读取权限；撤权、降权、退出 namespace、陈旧订阅与跨 namespace 需要 fail-closed。订阅入口是否复用 `VisibilityChecker` 同一访问合同也须纳入 source-first 核验。

按 `multica-engineering-delivery` v1.0.0 路由给 Planner R2，而非机械交给 Coder。Planner R2 必须重新建立 Source Contract / Repository Invariants / Plan Hypothesis，补全 producer → every consumer → eligibility filter → recipient-visible persistence/dispatch sink 的 changed-behavior graph，以及 PRIVATE、撤权/降权、陈旧订阅、跨 namespace、失败语义的 invariant matrix与可证伪验收。

截至本分录前内部 HEAD 为 `6407a647df42114c0e4f6eb5052efa83012ba1d0`、tree `3cea02cf72013c9798fa8071e5429da13197fd66`；upstream/main 与 fork/main 仍共同指向 `d2403bb5911953b8f53e62c3f0a9edc291363944`。未创建上游 PR。

## 交接

- 状态：REVIEW_RETURNED，返修从 Planner R2 开始
- 产物：本 superseding finding 与新 Planner Stage
- 下一步：Planner R2 source-first 重建行为图、不变量与 falsification 条件，经 Member Plan Gate 后再派 Coder
- 未解决：authorization/privacy/revocation/stale subscription/notification fan-out 不变量未被旧链验证
- 需要决策：Planner R2 产出后按 Project description 进入 Plan 人类 Gate
