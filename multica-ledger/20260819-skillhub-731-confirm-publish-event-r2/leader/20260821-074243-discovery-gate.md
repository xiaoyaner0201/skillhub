---
ts: 20260821-074243
agent: leader
action: discovery gate / plan dispatch
tree: 8c5b7249414f37b6e626dbe19bf5ce4b4382d9ed
verdict: PASS
---

对 `qa/20260821-073605-behavior-inventory.json` 的机械校验。全部为可复现的确定性检查，
不含语义裁决——inventory 的行为面是否**真的**完整，属 Planner disposition 与 Reviewer
blind audit 的职责，本 Gate 不代答。

## 校验项与实测

| # | 检查 | 期望 | 实测 | 结论 |
|---|---|---|---|---|
| 1 | 声明 commit 存在且为远端 head | `e3cc0fda…` | `e3cc0fdabe912941916961a7383c0726f4bc1bae` | PASS |
| 2 | 非 ledger 业务 diff（vs subject `23658e0f`） | 空 | 空 | PASS |
| 3 | 已有分录未被改写/删除 | 仅新增 | 2 个 `A` + `index.md` `M` | PASS |
| 4 | `index.md` 为纯表尾追加 | 无删除行 | +1 行，0 删除 | PASS |
| 5 | inventory SHA-256 与声明一致 | `4682fdd4…b5ec` | `4682fdd44d7ab56df887e403b4ab2ce8547ff8c8a1bb79385ab6be97f2deb5ec` | PASS |
| 6 | `subject.base_commit` == dispatch 指定 subject | `23658e0f…` | `23658e0f94f5d6ac1290978671a75eca5d56ef94` | PASS |
| 7 | `subject.base_tree` == dispatch 指定 tree | `d35a58c9…` | `d35a58c983b1ea3d0fa4830e2c0ca3bd43283673` | PASS |
| 8 | schema 校验 | PASS | `behavior-inventory.v1` PASS | PASS |
| 9 | `maturity` ≠ `D0` | 非 D0 | `D1` | PASS + 强制 warning |
| 10 | `candidate_id` 唯一 | 唯一 | 8/8 唯一 | PASS |
| 11 | provenance 字段齐备 | 齐备 | Run `b4535e97-7654-47ff-84c0-8c97cadedc18`；context `01a0233a-aea5-75a1-b194-715bdcf26ddd` | PASS |
| 12 | `command` 可复现 | 存在 | 确定性 `git grep -n -E …` | PASS |
| 13 | `coverage_note` 非空且陈述盲区 | 非空 | 744 字符 | PASS |

校验命令在 `/workspaces/HD-28/gates/` 执行。注意 `run_gate.sh` 的 `UV_CACHE_DIR` 回退依赖
`MULTICA_GATE_UV_CACHE_DIR`；直接调 `uv` 时环境里既有的 `UV_CACHE_DIR=/home/agent/.cache/uv`
会命中 noexec HOME 导致 `failed to map segment from shared object`，须显式指向
`/workspaces/.skill-cache/multica-gates-uv`。此为环境事实，记录以免复发。

## 强制 warning（随 receipt 传递）

`maturity=D1`。inventory **不是完整性上界**：

- Planner 须对 8 个 candidate 各 disposition **恰好一次**，落在 tripwire 模式外的行为面
  由 Planner 手工建模并说明依据，不得因「inventory 没列」而默认不存在；
- Reviewer 的 `BLIND_BEHAVIOR_AUDIT` 不得以本 inventory 为上界，仍须独立追踪。

## provenance 约束（传递给 Plan Gate）

Plan Run 的 `run_id` / `context_id` 必须 ≠ `b4535e97-7654-47ff-84c0-8c97cadedc18` /
`01a0233a-aea5-75a1-b194-715bdcf26ddd`，否则 `run_gate.sh --kind plan` 判自证并 RETURN。
本 Gate 由 leader Run `1dde911a-5873-46cf-8e5b-17259ef6d365` 执行，与 discovery Run 亦不同。

## 值得记录的发现

discovery 报出一个 dispatch 未点名的 candidate：`published-search-index-rebuild`
（`SearchIndexEventListener.onSkillPublished → PostgresSearchR…`）。它未被本 PR 修改，
但与 subscriber fan-out 共享同一 `SkillPublishedEvent`，正属「未改动却因新事件可达」的
行为面类别。Planner 不得以「不在 F1/F2 施工范围」为由排除——排除须给证据，且若触及
授权/隐私/隔离不变量则不得仅以 `non-goal` 排除。

## 交接

- 状态：Discovery Stage 关闭，PASS（附 D1 warning）；进入 Plan Stage
- 产物：本分录；inventory digest `4682fdd4…b5ec` 已冻结
- 工作分支：`20260819-skillhub-731-confirm-publish-event-r2` @ `e3cc0fda`
- 下一步：规划 产出 `plan-r1`（HD-28），含 `coverage_closure` 与 `council_nomination`
- 未解决：Publication 段（验收 5/6）仍待主控会话承接
- 需要决策：Plan 冻结后的 Plan Gate 审批，按 Project 路由 @ dongsjoa
