---
ts: 20260821-081557
agent: leader
action: HD-28 plan r1 gate
tree: ccc7320337592d8f66890ee5316dc3fd8d29f06a
verdict: PASS
---

对 `planner/20260821-075744-plan-gate.json` 的机械校验。Planner 已自跑一次 Gate；本分录是
**独立复跑**，不采信其自述结果。语义判断（方案是否正确、行为面是否真的闭合）不在本 Gate 内。

## 完整性与 append-only

| # | 检查 | 实测 | 结论 |
|---|---|---|---|
| 1 | 声明 commit == 远端 head | `02dd93468664ab2aba9594f26fd94b71134854a2` | PASS |
| 2 | 非 ledger 业务 diff（vs `f516f429`） | 空 | PASS |
| 3 | 变更全部落在 `multica-ledger/**` | 3 `A` + `index.md` `M` | PASS |
| 4 | `index.md` 纯表尾追加 | +1 行 / 0 删除 | PASS |
| 5 | inventory 未被修改 | `qa/` 无 diff；digest 仍 `4682fdd4…b5ec` | PASS |
| 6 | plan-r1.md digest 与声明一致 | `46d1441eb91f…5127a` | PASS |
| 7 | plan-gate.json digest 与声明一致 | `9408cbded5d0…b67d` | PASS |
| 8 | F3 草稿仅在 ledger 内 | `planner/20260821-075744-pr-body-draft.md` | PASS |

## validator 复跑

`run_gate.sh --kind plan` 独立执行，receipt：

- `result`: **PASS**
- `runs`: PLAN Run `d83deada-8fcf-43e8-a7e9-a0cb0219e4fd` / context `01a02347-46f7-7962-81ec-4118401867e2`
- provenance 隔离：与 discovery Run `b4535e97-…` / context `01a0233a-…` **均不同** ⇒ 非自证
- `inputs`: artifact `9408cbde…b67d`、inventory `4682fdd4…b5ec`
- `validator`: `validate_gate.py` v0.1，sha256 `a7100d86232e21df4f032d07ecbc7e055f2353020be0858230db131a47ca4892`
- `errors`: 空
- `warnings`: 1 条，D1「inventory is NOT an upper bound on behavior surface — Reviewer must trace independently」

## 闭包（复算，非读取声明）

inventory 8 个 candidate 与 artifact 声明集合逐一相等。`changed_behavior` 6 条 path 对
candidate 的映射**恰好一次**，无重复、无遗漏：

| path_id | candidates |
|---|---|
| `subscription-denial-localization` | `subscription-create-authz-and-response` |
| `retained-subscription-disclosure` | `retained-subscription-row-readback` |
| `published-owner-control` | `published-owner-notification` |
| `published-subscriber-observability` | `published-subscriber-fanout`, `published-namespace-missing-diagnostic` |
| `yanked-subscriber-observability` | `yanked-subscriber-fanout`, `yanked-namespace-missing-diagnostic` |
| `published-search-control` | `published-search-index-rebuild` |

`planned=8 / excluded=0 / unresolved=0`。discovery gate 点名的
`published-search-index-rebuild` 已建成 `published-search-control` 真实 path，未被以
`non-goal` 排除——符合上一 Gate 的传递约束。

## Council 名额校验（机械项，不替 Planner 选人）

| 检查 | 实测 | 结论 |
|---|---|---|
| 名额 2–3 | 3 | PASS |
| 锚点互异 | Schneier / Kleppmann / Beck，三者不同且检查偏好不重叠 | PASS |
| 含 `dissent_role` | `evidence-dissent`（Kent Beck-style）= true | PASS |
| `rationale` ≥30 字符 | 191 / 177 / 197 | PASS |
| `rationale` 引用具体 path/invariant 而非风险标签 | 每位 `referenced_path_ids` 3 条、`referenced_invariant_ids` 3 条 | PASS |
| `referenced_path_ids` 可解析到 planned 集合 | 3/3 全部命中，无悬空 ID | PASS |
| `must_answer` 3–5 条 | 4 / 4 / 5 | PASS |
| 未凑名额 | `declined_lenses` 2 条并附理由 | PASS |

## work_partition

单一单元 `hd28-targeted-remediation`，覆盖全部 6 条 planned path，`depends_on` 为空。
单元制下写集相交、成环、path 多头覆盖三项检查平凡通过。`no_split_rationale` 给出了
不拆的实质理由（共享 `skillhub-app` 测试模块、同一授权/扇出验收矩阵、须在候选修改前
冻结的 recipient tuple 基线），符合「不为并行而并行」。

## 探针可执行性抽查

20 条 `acceptance_probes` 全部为可执行命令，非描述性断言。

验收标准 2（非 null 情形收件人集合**逐元素一致**）落为 `published-nonnull-elementwise`
与 `yanked-nonnull-elementwise`：在 `23658e0f` 与候选 exact commit 两个 clean worktree
跑同一 fixture，序列化 ordered dispatcher tuple 后 `cmp`。这是差分证明，不是计数或描述。

额外抽查探针引用的测试是否真实存在（防止 Plan 引用不存在的入口）：12 个被点名的
class#method 全部命中现有代码，唯二不存在的是 `SubscriptionMessageBundleTest`（新建类）
与 `NotificationEventListenerTest` 的新增方法，二者的文件均已在 `write_set` 内，属预期新增。
`skillhub-search` 模块的三条探针为**控制组**（既有行为不得改变），故其文件不在 write_set，
一致。

## 传递给下游的约束

1. D1 warning 继续生效：Reviewer 的 `BLIND_BEHAVIOR_AUDIT` 不得以 inventory 为完整性上界。
2. F1 会修改 `.properties`，属真实业务变更；交付后业务 tree 必然不再等于 `e071afb4`，
   publication 分支同步属 Publication 责任面，不由本链执行。
3. Coder 的 Run/context 不受 validator 约束，但 QA Charter 与 Review Pass 1 必须 blind。

## 交接

- 状态：Plan Gate 机械校验 PASS；**待 Project 路由规定的人类审批**
- 产物：本分录；receipt 已复算，validator hash `a7100d86…4892`
- 工作分支：`20260819-skillhub-731-confirm-publish-event-r2` @ `02dd9346`
- 下一步：按 Project description，Plan Gate 由千乘妍独立审批；批准后派 实现 进入 Code/TDD
- 未解决：Publication 段（验收 5/6）仍待主控会话承接
- 需要决策：Plan Gate 审批（已按路由 @ dongsjoa，Issue 置 `in_review`）
