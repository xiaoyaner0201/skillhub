---
ts: 20260819-213047
agent: qa
action: union verification r3
tree: 2196fadc347cfdc844703df04877f7c388611a4f
verdict: PASS
---

## Verdict

`PLAN_COMPLIANCE=PASS`

`SYSTEM_BEHAVIOR=PASS`

对 exact subject tree 的独立复验未发现新的 `PLAN_GAP`、`IMPLEMENTATION_DEFECT`、`TEST_GAP`、`INVARIANT_VIOLATION` 或 `EVIDENCE_GAP`。QA R2 的最终 sink `TEST_GAP` 已由动态证据关闭：同一 production event 真实经过 production listener、production eligibility 与 production dispatcher，最终只为当前合格且非 publisher/actor recipient 创建 Notification，并向同 recipient 推送完整 SSE payload。

## Union 范围与实测行为

最终范围为 `R3 Charter ∪ frozen Plan R6 acceptance ∪ QA R2 finding closure ∪ existing regression`；独立章程见 `qa/20260819-212700-independent-charter-r3.md`。

- subscription PUT：合法 PUBLIC relation/counter/event 成功；inactive、archived namespace removed、PRIVATE ordinary/cross-namespace、hidden ordinary 与仅平台 SUPER_ADMIN 在 mutation 前拒绝；GET own boolean 与 DELETE stale cleanup 未继承 PUT policy。
- publish/yank：mixed candidates 中 stale/removed、inactive/missing、PRIVATE ordinary、cross-namespace、hidden ordinary、platform-only SUPER_ADMIN 与 publisher/actor 均未到达 sink；当前合格 ADMIN/member 精确到达。yank 覆盖 fallback/no-fallback，并验证 `wasPublished=false` 为 0 persistence/0 SSE。
- 最终链：`NotificationEventListener` → `SubscriptionRecipientEligibility` → `NotificationDispatcher` → `NotificationService.create` → SSE。精确断言 recipient、`PUBLISH` category、event type、title/body、entity type/id，以及 recipient-visible SSE 的 id/category/eventType/body/entity。
- fail closed：account、namespace、membership 三种 authority failure 均在首个 preference/persistence/SSE 前传播；0 partial fan-out。account/membership 批量调用各至多一次，无 per-recipient repository lookup；listener 未吞错。
- 兼容回归：PUBLIC archived direct detail/status、discovery、version/content/resolve、download/install、search、notification preference、payload/lifecycle/audit/outbox 相关 reactor tests 全绿；未观察 platform privilege bypass。

## 原始命令与结果

环境：Linux aarch64，Eclipse Temurin 21.0.12，Maven 3.9.13。

```text
精确 sink probe：
./mvnw -pl skillhub-app -am -Dtest=SubscriberNotificationSinkTest -Dsurefire.failIfNoSpecifiedTests=false test
SubscriberNotificationSinkTest: Tests run 8, failures 0, errors 0, skipped 0
BUILD SUCCESS

冻结 R6 定向并集（含 sink probe）：
./mvnw -pl skillhub-app -am -Dtest=SkillQueryServiceTest,SkillDownloadServiceTest,SkillSubscriptionServiceTest,SkillSubscriptionControllerTest,SkillGovernanceServiceTest,NotificationEventListenerTest,SubscriberNotificationSinkTest,*SubscriptionMetadataAccessPolicyTest,*SubscriptionRecipientEligibilityTest,NotificationDispatcherTest -Dsurefire.failIfNoSpecifiedTests=false test
domain: tests 76, failures 0, errors 0, skipped 0
notification: tests 4, failures 0, errors 0, skipped 0
app: tests 49, failures 0, errors 0, skipped 0
BUILD SUCCESS

全 reactor：
./mvnw -pl skillhub-domain,skillhub-infra,skillhub-auth,skillhub-notification,skillhub-search,skillhub-app -am test
storage 21/0/0/0; domain 415/0/0/0; auth 120/0/0/0;
notification 36/0/0/0; infra 10/0/0/0; search 35/0/0/0;
app 763/0/0/1; BUILD SUCCESS

唯一 skip：RedisClusterIntegrationTest，既有环境 gate；未配置 REDIS_CLUSTER_TEST_NODES。

git diff --check
exit 0, no output

git diff --quiet cc10ff21..65ac5c7f -- . ':(exclude)multica-ledger/**'
exit 0（subject 后仅 ledger/index 演进）
```

测试日志中出现的 S3/SSE/search failure stack traces 均来自显式故障注入用例；对应 suites 结果为 0 failures/0 errors。

## 覆盖边界与剩余风险

- 未连接真实数据库、真实浏览器 EventSource、Redis cluster 或生产基础设施；repository/persistence 与 SSE transport 是 mock 边界。
- 未执行并发、负载、跨进程投递或部署验证。
- 这些边界不阻断本轮：生产 listener/eligibility/dispatcher 的同步资格与最终 sink 合同已动态贯通，全 reactor 同时覆盖相关持久化、SSE、search、audit 与兼容行为；Redis skip 是既有显式环境 gate。

## Provenance 与交接

- 被验证业务/测试 commit：`cc10ff21efa507adf095fbe68d38b5a251542c0e`
- 被验证 exact subject tree：`2196fadc347cfdc844703df04877f7c388611a4f`
- 取件 HEAD/tree：`65ac5c7fe2a8e47b7861553265e2c2999c642c4d` / `1127f83ea8b20cb1a2cf0e638aa2f061d4f1cd3c`
- 状态：QA R3 独立验证完成，双 PASS；后续任一非 ledger 文件变化使本结论失效。
- 下一步：Stage barrier 进入 fresh-context Reviewer。
- 未解决：无 blocker；仅上述明确未覆盖项。
- 需要决策：无。
