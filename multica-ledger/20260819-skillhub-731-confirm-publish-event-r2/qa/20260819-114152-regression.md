---
ts: 20260819-114152
agent: qa
action: regression
tree: ce971b514f35d159ea50a3fe0bfe24cd359ef2d3
verdict: PASS
---

## 结论

对候选 HEAD `0d9c8eedcffb22fecef846324569fa58815ea709`、exact tree
`ce971b514f35d159ea50a3fe0bfe24cd359ef2d3` 独立动态验证，verdict 为 PASS。

## 环境准备

运行环境为 Linux aarch64，使用 Temurin 21.0.12。Maven 单模块命令不自动构建
`skillhub-storage`，因此先安装未修改的父 POM 与 `skillhub-storage` 到本地仓库：

```text
$ ./mvnw -N install
[INFO] BUILD SUCCESS

$ ./mvnw -pl skillhub-storage install -DskipTests
[INFO] Tests are skipped.
[INFO] BUILD SUCCESS
```

这两步只生成构建产物，未修改候选生产代码或测试代码。

## 原始执行证据

```text
$ cd server && ./mvnw -pl skillhub-domain -Dtest=SkillReviewSubmitServiceTest test
[INFO] Running com.iflytek.skillhub.domain.skill.service.SkillReviewSubmitServiceTest$ConfirmPublishTests
[INFO] Tests run: 6, Failures: 0, Errors: 0, Skipped: 0
[INFO] Running com.iflytek.skillhub.domain.skill.service.SkillReviewSubmitServiceTest$SubmitForReviewTests
[INFO] Tests run: 4, Failures: 0, Errors: 0, Skipped: 0
[INFO] Results:
[INFO] Tests run: 10, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
[INFO] Total time:  8.747 s
```

```text
$ cd server && ./mvnw -pl skillhub-domain test
[INFO] Results:
[INFO] Tests run: 411, Failures: 0, Errors: 0, Skipped: 0
[INFO] BUILD SUCCESS
[INFO] Total time:  3.612 s
```

## 覆盖核验

- 成功路径验证事件恰好发布一次，并逐项断言 `skillId`、`versionId`、
  `publisherId` payload。
- 非 PRIVATE skill 与非法 version 状态两类前置校验失败均验证零事件。
- version save 抛错与 skill save 抛错均验证异常透传及零事件；后者还验证
  version 已尝试保存，事件仍未发布。
- `submitForReview` 的正常路径验证零事件。
- 从冻结 BASE 到候选的业务 diff 仅触及
  `SkillReviewSubmitService.java` 与对应测试；未触及 PUBLIC 直发、yank、权限过滤、
  监听器或其他业务路径。其余 diff 均为既有 Multica 账本。

## 未覆盖

- 未运行全仓库 reactor、集成测试、端到端测试或外部消息/监听器联调。
- 未验证真实数据库事务提交后的事件时序；本轮是 domain 单元测试与模块回归。
- 未做语义或架构评审。
