---
ts: 20260819-211005
agent: qa
action: union verification r2
tree: 71d9c55387b0a0b8336905d3aa9986fff6be6c97
verdict: FAIL
---

## Verdict

`PLAN_COMPLIANCE=FAIL`  
`SYSTEM_BEHAVIOR=FAIL`

候选的现有动态探针全部通过，未复现授权泄漏或 partial fan-out；但冻结 R6 明确要求的真实 listener → dispatcher → Notification persistence → recipient SSE 端到端资格探针不存在。当前测试把 listener 的最终边界 mock 成 `NotificationDispatcher`，另一个独立测试直接调用 dispatcher；两段测试没有证明同一生产事件、同一 eligibility 结果最终只为合格 recipient 创建 Notification 并推送 SSE。权限/通知风险面不能以两段 mock 链外推为系统 PASS，因此结论为 `TEST_GAP`，不是已证实的生产实现缺陷。

## Union 范围

最终范围为 `Independent Charter ∪ frozen plan-r6 acceptance ∪ existing reactor regression`。章程见 `qa/20260819-210445-independent-charter-r2.md`。

## 实测行为

- subscription PUT 的现有探针验证：合法 PUBLIC 创建 relation/counter/event；inactive、archived namespace removed、PRIVATE member/nonmember/cross-namespace 与 hidden MEMBER 在 mutation 前拒绝；PUBLIC archived skill 可订阅；GET/DELETE 不读取 metadata authorization dependencies。
- publish/yank listener 探针验证：inactive/missing/stale 候选过滤；PRIVATE mixed batch 仅 ADMIN 收到且 publisher 排除；yank `wasPublished=true/false`、actor 排除、archived namespace current/removed；account、namespace、membership 三种读取异常均在 mocked dispatcher 首次调用前失败。
- dispatcher 探针验证：给定 subscriber 参数时，以 exact recipient/event type/body 创建 Notification，并向同一 recipient 推送含 id/category/eventType/body/entity 的 SSE map；preference disabled 时不持久化、不推送。
- compatibility 探针验证：PUBLIC archived direct detail 保持成功且 status=ARCHIVED；受保护 version/content/resolve/download 合同与 platform SUPER_ADMIN 无普通 read bypass 的相关测试通过。
- 静态核验：account/membership 使用各一次 batch repository call 的生产路径，没有 per-recipient repository lookup；异常未被 listener 捕获吞没。`f3decf81..0b666aca` 排除 ledger 后 diff 为空。

## Material finding

### TEST_GAP — 真实最终 sink 链未闭合

- prerequisites：至少一个合格 subscriber 与一个不合格 subscriber，PUBLISH preference enabled，真实 `NotificationDispatcher`（其下 mock persistence/SSE 边界）。
- events：触发 `SkillPublishedEvent` 或 `SkillVersionYankedEvent`。
- call chain：`NotificationEventListener` → `SubscriptionRecipientEligibility` → `NotificationDispatcher` → `NotificationService.create` → `SseEmitterManager.push`。
- state/sink：仅合格、非 publisher/actor recipient 得到 exact Notification persistence 参数和 recipient-visible SSE；不合格 recipient 两者均为 0；任一 authoritative read failure 整批两者均为 0。
- visible result：现有 `NotificationEventListenerTest` 在 dispatcher 处终止，`NotificationDispatcherTest` 从手工 `dispatch(...)` 开始；没有测试执行上述完整 call chain，故最终 recipient 资格与 persistence/SSE 不能由动态证据确认。

冻结 Plan 还要求按 publish/yank fallback/no-fallback 覆盖 inactive、PRIVATE current/removed、cross-namespace、hidden、platform-only SUPER_ADMIN 与 batch failures 的矩阵。现有 listener 动态集只覆盖其中组合子集；尤其没有 subscriber fan-out 的 platform-only SUPER_ADMIN/hidden/cross-namespace 明名动态用例，也没有真实 sink 上的 fail-closed 断言。这是同一主类 `TEST_GAP`。

## 原始命令与结果

环境：Linux aarch64，Temurin 21.0.12，Maven 3.9.13。

```text
定向：mvn -pl skillhub-app -am -Dtest=SkillQueryServiceTest,SkillDownloadServiceTest,SkillSubscriptionServiceTest,SkillSubscriptionControllerTest,SkillGovernanceServiceTest,NotificationEventListenerTest,*SubscriptionMetadataAccessPolicyTest,*SubscriptionRecipientEligibilityTest,NotificationDispatcherTest -Dsurefire.failIfNoSpecifiedTests=false test
domain: Tests run 76, failures 0, errors 0, skipped 0
notification: Tests run 4, failures 0, errors 0, skipped 0
app: Tests run 41, failures 0, errors 0, skipped 0
BUILD SUCCESS

全回归：mvn -pl skillhub-domain,skillhub-infra,skillhub-auth,skillhub-notification,skillhub-search,skillhub-app -am test
storage 21/0/0/0; domain 415/0/0/0; auth 120/0/0/0;
notification 36/0/0/0; infra 10/0/0/0; search 35/0/0/0;
app 755/0/0/1; BUILD SUCCESS
唯一 skip：RedisClusterIntegrationTest，因 REDIS_CLUSTER_TEST_NODES 未配置的既有环境 gate。

git diff --check
exit 0, no output
```

首次 wrapper 调用因 `/home/agent` 为 noexec 而 exit 126；随后在 `/workspaces` 使用相同 Temurin/Maven 工具链完成上述有效运行。该环境预检失败不计入产品测试 verdict。

## 覆盖边界

- 未连接真实数据库、浏览器 SSE、Redis cluster 或生产基础设施。
- 未执行并发、负载或 soak。
- 未修改候选生产代码或测试代码；仅追加 QA 账本与 index 行。

## Provenance 与交接

- 被验证业务 commit：`f3decf8147fe1ef8809cac21afce3a5b77d607bb`
- 被验证 subject tree：`71d9c55387b0a0b8336905d3aa9986fff6be6c97`
- 取件 HEAD/tree：`0b666aca3130f814440faed415bdd43a905f3ffd` / `3acab151566a4f816e6e88771ffad760bddf8175`
- 状态：QA 动态验证职责完成，FAIL 路由 Coder 补真实最终 sink 与缺失授权矩阵探针后重做 QA。
- 未解决：上述 `TEST_GAP`。
- 需要决策：无。
