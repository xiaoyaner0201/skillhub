---
ts: 20260819-205859
agent: coder
action: regression
tree: 71d9c55387b0a0b8336905d3aa9986fff6be6c97
verdict: —
---

在 Temurin JDK 21.0.12（Linux aarch64，下载包 SHA-256 校验通过）下对 R6 最终业务
tree `71d9c55387b0a0b8336905d3aa9986fff6be6c97` 执行冻结验证。

## 动态证据

- 首次定向运行揭示此前未执行测试中的两个 test-only defect：
  `SkillSubscriptionServiceTest` 缺少三个 domain type import，导致 testCompile 失败；
  修正后 `NotificationEventListenerTest` 的 membership-batch-failure 用例暴露多余 namespace
  slug stubbing，Mockito strict mode 报错。这两项均在允许的 test 写集中做最小修正，未改生产行为。
- 最终定向命令：冻结 R6 §6 原命令；BUILD SUCCESS。domain 76 tests、notification 4 tests、
  app 41 tests，均 failures=0/errors=0/skips=0。
- 最终全回归命令：冻结 R6 §6 原命令；BUILD SUCCESS。app 755 tests，
  failures=0/errors=0/skips=1；其余 reactor modules 全部 SUCCESS。唯一 skip 为既有
  `RedisClusterIntegrationTest` 环境型跳过。
- `git diff --check` 通过。测试日志未纳入提交。

## 写集与边界

本轮仅修正：

- `server/skillhub-app/src/test/java/com/iflytek/skillhub/domain/social/SkillSubscriptionServiceTest.java`
- `server/skillhub-app/src/test/java/com/iflytek/skillhub/listener/NotificationEventListenerTest.java`

生产代码、schema、payload、lifecycle、search/preference 与受保护 read 合同均未变化；未触发
R6 falsification / `PLAN_GAP`。

## 交接

- 状态：Coder R6 冻结实现、测试矩阵与本地动态回归已完成；不自签 QA/Review PASS。
- 产物：test fix commit `f3decf8147fe1ef8809cac21afce3a5b77d607bb`；subject tree
  `71d9c55387b0a0b8336905d3aa9986fff6be6c97`。
- 下一步：推送内部 fork 分支并由 Stage barrier 进入独立 QA Charter。
- 未解决：无。
- 需要决策：无。
