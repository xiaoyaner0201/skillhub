---
ts: 20260819-212121
agent: coder
action: TEST_GAP RED
tree: 71d9c55387b0a0b8336905d3aa9986fff6be6c97
verdict: —
---

## RED 分类

QA R2 finding 是 `TEST_GAP`，不是已证实的生产缺陷。新增
`SubscriberNotificationSinkTest` 前，subject tree 上不存在能把同一生产事件沿真实
`NotificationEventListener` → `SubscriptionRecipientEligibility` →
`NotificationDispatcher` 执行到 `NotificationService.create` 与 SSE 的测试命题。

因此本轮 RED 是 coverage RED：新增测试类在旧 subject tree
`71d9c55387b0a0b8336905d3aa9986fff6be6c97` 上无法执行（测试文件不存在），而不是伪造
行为失败。新增探针首次在当前生产实现上运行即 GREEN，证明未发现
`IMPLEMENTATION_DEFECT`；没有修改生产代码。

初始执行：`SubscriberNotificationSinkTest` 共 7 个实例，0 failure / 0 error；随后补齐独立
hidden 用例后为 8 个实例。首次 wrapper 因 `/home/agent` noexec 返回 126；改用
`/workspaces` 下 Temurin 21.0.12 与 Maven 3.9.13 后完成有效运行。

## 探针命题

- publish mixed：PRIVATE current ADMIN 唯一 persist/SSE；publisher、撤权、inactive、missing、
  PRIVATE MEMBER、cross-namespace 与 platform-only SUPER_ADMIN 均为 0。
- hidden publish：current ADMIN 唯一 persist/SSE；ordinary member 与 platform-only
  SUPER_ADMIN 为 0。
- yank fallback/no-fallback：archived namespace current member 唯一 persist/SSE；actor、removed、
  inactive、missing 为 0；`wasPublished=false` 整批为 0。
- account、namespace、membership 三类 batch failure 均在首个 preference/persistence/SSE sink
  前抛出，且 batch invocation count 为 0/1 的预期值，无 N+1、无吞错。

## 交接

- 状态：coverage RED 已记录；当前实现未暴露生产缺陷
- 产物：`server/skillhub-app/src/test/java/com/iflytek/skillhub/listener/SubscriberNotificationSinkTest.java`
- 下一步：记录 GREEN 与全回归，推送同一内部分支
- 未解决：无
- 需要决策：无
