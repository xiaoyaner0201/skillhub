---
ts: 20260821-111250
agent: coder
action: HD-28 R5 TDD RED / R8 compatibility probe
tree: e8aca2a43467dad0da969c182632a7b791f90621
verdict: —
---

## RED

取件前先 `git fetch`；远端内部分支 head 仍为
`607d8541d1f4479bf35ae54ce75afd68ff540ece`，与重派声明一致。provider 失败 run
`d1650051` 没有形成业务提交；本轮以该 head 重新执行 M3b 生效计划。

R5 的 publish/yank 两条真实 logger + ListAppender probe 先落测试，再对生产实现运行：

- publish：`Tests run: 2, Failures: 1`，预期 1 条带 ID 的 WARN，实际 0；
- yank：`Tests run: 2, Failures: 1`，同样预期 1、实际 0；
- 两枚 probe 同时捕获固定 `IllegalStateException`，要求原 throwable identity、不含
  `subscriber-pii-sentinel` 且 dispatcher 零次。旧实现只重抛、没有该 WARN，因此按目标缺陷失败。

R8 是 mutation sentinel，不伪报 RED：三类既有 denial case 改为直接断言
`DomainForbiddenException.messageCode()` 后，在修复前 tree 运行 `6/0/0/0`，
`BUILD SUCCESS`。它是明确的 `PASS → PASS` 兼容探针；保留 repository、count 与 event
零 mutation 断言，未使用 `hasMessage()` / `getMessage()`。

owner、两条非 null fan-out 与 search 同样是控制组，不记为 RED。

## 交接

- 状态：R5 两条目标 RED 已按预期建立；R8 compatibility probe 修复前 PASS
- subject：修复前业务 tree `e8aca2a43467dad0da969c182632a7b791f90621`
- 下一步：仅在两次 `NamespaceRepository.findById` 周围加窄 catch，WARN 后原样重抛
- 未解决：GREEN、六元组差分与回归待执行
- 需要决策：无
