---
ts: 20260819-114706
agent: reviewer
action: exact-tree review
tree: 6ebe64036cdddb4dc85ef4127de39bb5bcaa3e83
verdict: PASS
---

## 结论

对候选 HEAD `068db0cdeb80d19041d5107f44667b6d66df8853`、exact tree
`6ebe64036cdddb4dc85ef4127de39bb5bcaa3e83` 做 fresh-context 只读评审，verdict 为 PASS。
未发现需要返修的语义、架构、安全或隐私 finding。

## 关键语义

- 从冻结 BASE 到候选，业务写集仅有 `SkillReviewSubmitService.java` 与其对应测试；
  QA 候选后的两次提交仅追加 QA/Leader 账本与 index 变化。
- `confirmPublish` 先保存状态为 `PUBLISHED` 的 version，再更新并保存 skill，最后恰好
  调用一次 `publishEvent`。payload 取 `skill.getId()`、`version.getId()` 与本次操作的
  `actorUserId`，没有信任未经实体核验的请求 ID 作为事件实体标识。
- version save 或 skill save 抛错都会在发布点之前中止；前置 visibility/status 校验失败
  同样不会到达发布点。`submitForReview` 未新增事件调用。
- 实现沿用 `SkillPublishService`、`ReviewService` 与 `PromotionService` 的同步
  `ApplicationEventPublisher` 模式，监听器使用事务事件语义；没有引入新的耦合、事件
  类型、权限边界或凭据处理。

## 测试审查

- 成功测试捕获 `SkillPublishedEvent` 并以 `times(1)` 精确验证 payload；两类持久化失败
  分别让对应 mock 抛错并验证零事件，断言没有被 Mockito 默认返回值掩盖。
- 两类既有 `confirmPublish` 校验失败均验证零事件；成功 `submitForReview` 也锁定零事件。
- 测试没有用调用顺序对象显式断言顺序，但生产代码控制流、两个 save 异常边界测试与
  成功调用验证共同足以证明本次四行实现的发布点位于两次 save 之后，不构成缺口。

## 风险与未审范围

- QA 未跑全仓 reactor、集成/E2E、监听器联调，也未以真实数据库验证事务提交时序。
  这不阻止 PASS：本改动复用现存事件类型和同类生产模式，domain 模块 411 个测试已过，
  且 `@TransactionalEventListener` 会在事务提交后消费事件。跨进程可靠投递/outbox 不是
  #731 或冻结验收范围。
- 订阅者权限过滤的潜在产品策略、PUBLIC 直发、yank、事件体系重构均未审，且本候选
  未改变这些边界。
- GitHub CLI 在本环境未认证，因此未直接读取 live issue 页面；评审契约、冻结计划、
  exact Git diff 和仓库内实现是本结论的事实源。

## 交接

- 状态：PASS，等待 Member 对 exact tree
  `6ebe64036cdddb4dc85ef4127de39bb5bcaa3e83` 做最终 Review Gate 决策。
- 产物：本 reviewer verdict；候选代码与测试未修改。
- 下一步：批准则 reviewer 子任务完成并由 Stage barrier 唤醒父任务；退回则把具体要求
  转为 RETURN 并路由父任务返修。
- 未解决：无阻断项。
- 需要决策：Member 批准或退回当前 exact tree。
