---
ts: 20260819-214118
agent: reviewer
action: exact-tree review r3
tree: 2196fadc347cfdc844703df04877f7c388611a4f
verdict: PASS
---

## Verdict

- Intent: **PASS**
- Invariant/Security: **PASS**
- Evidence: **PASS**
- Exact Tree: **PASS**
- Overall: **PASS**

本 verdict 只对业务/测试 commit `cc10ff21efa507adf095fbe68d38b5a251542c0e`、exact subject tree `2196fadc347cfdc844703df04877f7c388611a4f` 有效。未发现未解决的 blocker/high finding。

## Pass 1：Source-first changed-behavior review

### Source Contract 与 changed-behavior graph

live `iflytek/skillhub#731` 要求 `confirmPublish` 在 version 与 skill 持久化成功后产生 `SkillPublishedEvent`，使既有 subscriber listener 扇出更新通知，同时明确要求复核当前 read permission。独立追踪的生产路径为：

```text
confirmPublish
→ save PUBLISHED version
→ save skill.latestVersionId
→ SkillPublishedEvent
├─ owner notification listener
├─ subscriber listener
│  → subscription candidates
│  → current namespace
│  → batch active accounts + batch fresh namespace memberships
│  → metadata-purpose eligibility
│  → publisher exclusion
│  → NotificationDispatcher
│  → preference → Notification persistence → SSE
└─ SearchIndexEventListener(AFTER_COMMIT) → search rebuild

yankVersion(pre-state=PUBLISHED)
→ save YANKED + recompute latest fallback
→ SkillVersionYankedEvent(wasPublished=true)
→ subscriber listener
→ yanked-publication eligibility using current account/membership/visibility
→ actor exclusion → persistence/SSE

PUT subscription
→ load skill/account/namespace/current membership
→ metadata-purpose authorization
→ relation/counter/event mutation
```

`submitForReview` 不产生该事件；validation 或任一显式 save 抛错时 `confirmPublish` 不走发布调用。`SkillPublishedEvent` 的其他 producers、owner listener 与 search consumer 保持兼容；新增 yank 字段的生产构造点完整且只携带最小 pre-yank publication fact。

### Invariant/Security

`SubscriptionMetadataAccessPolicy` 与仓库实际 `VisibilityChecker` 的 protected metadata 规则一致：inactive/missing account、missing namespace、archived namespace removed user、hidden ordinary user、unpublished non-owner、NAMESPACE_ONLY nonmember、PRIVATE ordinary member/nonmember、cross-namespace 与仅 platform SUPER_ADMIN 均 fail closed；PRIVATE owner/当前 ADMIN/OWNER 和 NAMESPACE_ONLY 当前 member 可继续。fan-out 先完整读取 account 与 membership facts，再开始逐 recipient dispatch；读取异常不会产生 partial persistence/SSE，且 repository 调用为 batch，不是 N+1。

purpose 边界没有泄漏到 GET/DELETE、detail、version/content/resolve、download/install、search、preference 或 payload 合同。PUBLIC archived direct detail 与 metadata compatibility 被保留，content/install 的更严格 ACTIVE gate未被放松。yank 用 `wasPublished` 避免在 no-fallback 的 post-yank `latestVersionId=null` 上误拒合法历史通知，同时不携带陈旧角色或授权 snapshot。

## Pass 2：Challenge Plan 与 QA

Plan R6 的 entry-purpose 划分与 live source 相符，没有用统一 read helper 错误收紧 archived PUBLIC detail，也没有给 platform role 新增普通 portal read bypass。QA R3 的 Independent Charter 在采用 Plan/closure 前绑定同一 subject tree，范围是独立章程、Plan R6、QA R2 `TEST_GAP` 与既有回归的并集。

QA R2 指出的证据缺口已被 `SubscriberNotificationSinkTest` 关闭：测试实例化 production listener、production eligibility 与 production dispatcher，只在 repository、Notification persistence 和 SSE transport 边界 mock；它在同一事件路径断言 exact eligible recipient、category/event type、title/body/entity、Notification create 与 recipient-visible SSE，并覆盖 account/namespace/membership failure 的零 side effect。confirm-publish 测试精确断言事件 payload、一次发布、失败零事件；旧行为缺少该 publish call，RED 对真实旧行为有效。

QA 记录的定向与全 reactor 结果均为零 failure/error；唯一 skip 是既有 Redis cluster 环境 gate。Reviewer 尝试风险驱动复跑时，本 fresh runtime 没有可执行 JDK，且 wrapper cache 位于 noexec 路径，未生成新的动态结果；这不构成 blocker，因为 QA 已给出绑定 exact tree 的完整命令、模块计数与 sink evidence，Reviewer 的静态 source-first 审查未发现与其冲突的事实。

## Exact Tree / Provenance

- BASE：`d2403bb5911953b8f53e62c3f0a9edc291363944`
- subject commit：`cc10ff21efa507adf095fbe68d38b5a251542c0e`
- subject tree：`2196fadc347cfdc844703df04877f7c388611a4f`
- 取件 HEAD：`2b9d3980abe89b1f6c399b6a62e6a57b6ec002af`
- 取件 tree：`7336b9919b40612d069d3151a4ca2844be50ac8b`
- `git diff --quiet cc10ff21..2b9d3980 -- . ':(exclude)multica-ledger/**'`：exit 0；subject 后只有 ledger/index 演进。
- `git diff --check BASE..subject -- . ':(exclude)multica-ledger/**'`：exit 0。
- Reviewer 审查期间候选生产/测试代码零修改；仅追加本 reviewer 分录与 index 表尾行。

## 未审范围与剩余风险

未连接真实数据库、浏览器 EventSource、Redis cluster或生产基础设施；未做并发、负载、跨进程投递与部署验证。SSE push 失败仍沿既有语义：Notification 已持久化，push 为非关键失败。上述均不是本 exact tree 的新 blocker。

## 交接

- 状态：四维 PASS，进入最终 Review 人类 Gate。
- 产物：本 verdict，绑定 subject tree `2196fadc347cfdc844703df04877f7c388611a4f`。
- 下一步：请 Project 授权审批人对当前 exact revision 决定 APPROVE 或 RETURN；APPROVE 后追加批准审计并置 Reviewer Issue done，RETURN 后按具体 finding 路由返修。
- 未解决：仅最终 Review 人类决策与后续 Publication Gate。
- 需要决策：APPROVE 或 RETURN 当前 exact subject tree。
