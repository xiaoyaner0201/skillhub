# Stage 1 RED — coder 分录

- 时间：2026-08-24T07:43:26Z
- Agent：实现 · Coder（`fd2d1252-2974-4998-8b7e-128c7c7c2c36`）
- 起点：`origin/20260824-hd32-skillhub-735-subscription-authz-hardening` @ `744193d7`（dispatch 指定，已读回一致）
- 工单：`leader/20260824-044027-stage1-red-workorder.json` sha256 `876014dae72a723775e2b384f3c9aa3905ce55dcc313bae4bf3cdc528968fc86`
- 冻结 artifact：`planner/20260824-040000-plan-gate-r1.json` sha256 `bdfab21ffa1c7c7df05a4478d26d035329cf36c24a1d91b981c0f85e51a92b45`
- 结论：**RED PARTIAL + PLAN_GAP**（21 条探针中 15 条落地为断言级 RED，6 条在 Stage 1 边界内不可表达）

## 冻结输入自证

独立复算（`git show <branch>:<path> | sha256sum`）：四份冻结产物 digest 与 index.md 记录一致，无漂移。
工单为冻结 artifact 的机械投影：artifact `acceptance_probes` 共 41 条，`expected_before=="FAIL"` 恰 21 条，
与工单 21 条 id 集合对称差为空；`test_files_touched` 5 个文件全部位于 `src/test/java/**`。

## 交付

| 文件 | 探针 | 结果 | commit | tree |
|---|---|---|---|---|
| `server/skillhub-app/src/test/java/com/iflytek/skillhub/exception/SubscriptionMessageBundleTest.java`（新增） | 1 | Tests run: 1, Failures: 1 | `bfccbc0c49d8a6846072f6ae0cf4a2295d866ce4` | `681c2c1e995cc40dc38fcdaacd9befc759882e72` |
| `server/skillhub-app/src/test/java/com/iflytek/skillhub/domain/social/SkillSubscriptionServiceTest.java`（追加） | 3 | Tests run: 9, Failures: 3 | `5bb313b8935a69052b1dff04d1ffbc96d2aabbdd` | `33d808c06f4f0d432b4c71338d073fc94fc9f2a2` |
| `server/skillhub-app/src/test/java/com/iflytek/skillhub/service/MySkillAppServiceTest.java`（追加） | 4 | Tests run: 16, Failures: 4 | `c545fb3ef130e646deef734067a73d0f6c9a7b95` | `440753d35b27825009dd2fa0b06e46279117bee8` |
| `server/skillhub-app/src/test/java/com/iflytek/skillhub/listener/SubscriberNotificationSinkTest.java`（新增） | 7 | Tests run: 7, Failures: 7 | `44b71e0e1d160b2a121fd935dbc881128aa580ab` | `ca28196b0bfda8860ba6e30a5562f39688176c78` |

候选 exact tree：commit `44b71e0e1d160b2a121fd935dbc881128aa580ab` / tree `ca28196b0bfda8860ba6e30a5562f39688176c78`（本分录提交前）。
`src/main/` 零改动；`git status` clean；全部已推送 fork `origin`。

### 合并复跑（单次命令，全部四个类）

```
./mvnw -pl skillhub-app -am \
  -Dtest='SubscriptionMessageBundleTest,SkillSubscriptionServiceTest,MySkillAppServiceTest,SubscriberNotificationSinkTest' \
  -Dsurefire.failIfNoSpecifiedTests=false test
```

```
[ERROR] Tests run: 33, Failures: 15, Errors: 0, Skipped: 0
```

**Errors: 0** 是本阶段验收的关键：15 条全部为 `java.lang.AssertionError`（断言/契约不满足），
无编译错误、无 `ClassNotFoundException`；33−15=18 条既有测试保持通过，未被新增探针污染。

代表性失败原文：

- bundle：`[English subscription denial copy must not fall back to the raw code] expected: not equal to "error.skill.subscription.noPermission"`
- subscribe：`java.lang.AssertionError: Expecting code to raise a throwable`（`assertDeniedWithNoSideEffect:140`）
- /me/subscriptions：`Expecting actual: ["private-skill", "public-skill"] to contain exactly (and in same order): ["public-skill"]`
- fanout：`[no durable notification row may be written for ordinary-1] Expecting no elements of: [...] to match given predicate but this element did`

## 探针落地明细

已落地断言级 RED（15）：
`subscribe-private-denied`、`subscribe-hidden-denied`、`subscribe-removed-denied`、
`subscribe-message-en-zh`、
`stale-read-private-denied`、`stale-read-removed-denied`、`stale-read-hidden-denied`、`stale-read-mixed-visible-total`、
`published-private-denied`、`published-hidden-denied`、`published-removed-denied`、
`yanked-private-denied`、`yanked-hidden-denied`、`yanked-removed-denied`、
`notification-ineligible-zero`。

未落地（6）：见下 PLAN_GAP。

测试均贴最终生产入口与 side-effect sink：
`SkillSubscriptionService.subscribe`（sink：订阅行 / `incrementSubscriptionCount` / `SkillSubscribedEvent`）、
`MySkillAppService.listMySubscriptions`（sink：`PageResponse` 内容与 total，并断言留存行未被清理，守 `NON_GOAL_NO_DATA_CLEANUP`）、
`NotificationEventListener.onSkillPublishedForSubscribers` / `onSkillVersionYankedForSubscribers`
（sink：`NotificationRepository.save` 持久行 + `SseEmitterManager.push` 传输）。
fanout 用例串接真实 `NotificationDispatcher` / `NotificationService` / `NotificationPreferenceService`，
不打桩中间层，避免"只验证一个未被真实入口调用的 helper"。

## PLAN_GAP（停止扩展，交回 leader 裁决）

Stage 1 边界为「只写 5 个测试文件，不得改任何 `src/main/`」，且「编译错误或 `ClassNotFoundException` 不算 RED」。
在该边界内，以下 6 条探针不可能产出断言级 RED：

**G1 — `SubscriberAccessResolverTest.java` 的 2 条（`authority-removed-member`、`authority-disabled-user`）**
被测类 `server/skillhub-app/src/main/java/com/iflytek/skillhub/listener/SubscriberAccessResolver.java`
在计划 `write_set` 内，但**在 base 上不存在**（`git ls-tree` 已核对；该路径仅存在于 r3 分支提交 `046c04ed`，
而 r3 的 ledger 结论来自已删除任务，不得作为依据）。测试引用它只能得到编译错误——按 dispatch 定义不算 RED。
因此本文件**未创建**，未凑数、未以任何替身伪装成 RED。

**G2 — 四条 `*-disabled-*`（`subscribe-disabled-denied`、`stale-read-disabled-denied`、`published-disabled-denied`、`yanked-disabled-denied`）**
"账号停用"这一事实，只能经由生产协作者注入；base 上三个被测入口都不持有任何账号状态协作者
（`SkillSubscriptionService` 三参构造、`MySkillAppService` 与 `NotificationEventListener` 的依赖集合中均无）。
用未打桩 mock 制造的"查不到用户"表达的是**另一条不变量**（账号缺失 → fail-closed），
把它当作 disabled-user 覆盖上报属于误报，故不写。

两处较轻的偏差，一并披露（未自行填补）：

- **D1 —** `server/skillhub-app/src/test/java/com/iflytek/skillhub/notification/sse/RecordingSseEmitterTestConfiguration.java`
  在计划 `write_set` 内，但不在 Stage 1 的 5 个 `test_files_touched` 内。为不越界，SSE 录制改为
  `SubscriberNotificationSinkTest` 内自洽实现（`SseEmitterManager` mock + 自建 recording `NotificationRepository`）。
  若后续 Stage 仍要求该共享 fixture，需由 leader 显式排期。
- **D2 —** removed-member 类探针的**正向对照**用 owner 而非"当前 MEMBER"。base 上这三个入口都不接收成员关系协作者，
  "当前 MEMBER 可读"在单元层不可表达。对照仍然有效（证明拒绝不是全量静默），但覆盖面弱于计划设想，
  真正的成员维度需落在集成层或 GREEN 阶段引入协作者之后。
- **D3 —** `ASSUME_MESSAGE_COPY` 声称 EN/zh 文案在 plan-r1 中冻结，实际 plan-r1 只要求两个 locale 真实解析、
  未冻结字面串。故 bundle 探针断言的是**冻结的可观察量**（不回落到裸 code、EN≠ZH），而非字面文案。

## 工具链

`/home/agent` 为 `tmpfs ... noexec`，Maven wrapper 分发的 `bin/mvn` 因此 exit 126（`chmod +x` 无效）。
已将 wrapper 与本地仓库复制到可执行的持久卷并改用：

```
JAVA_HOME=/workspaces/HD-32/tooling/jdk            # Temurin 21.0.12+8
MAVEN_USER_HOME=/workspaces/HD-32/tooling/m2
MAVEN_OPTS="-Djansi.tmpdir=/workspaces/HD-32/tooling/jansi -Djansi.force=false"
```

此为运行环境适配，未改动仓库内任何构建配置。

## 边界自查

未扩权、未改 non-goals、未改 `src/main/`、未 push 公开分支、未建上游 PR、未签 CLA、未改写作者、未 force-push。
未自签 PASS：本分录只陈述实测事实，Stage 1 是否通过由 leader 裁决。
