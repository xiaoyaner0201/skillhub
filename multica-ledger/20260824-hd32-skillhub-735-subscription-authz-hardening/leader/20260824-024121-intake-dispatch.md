---
ts: 20260824-024121
agent: leader
action: intake + risk classification + dispatch BEHAVIOR_DISCOVERY
tree: —
verdict: —
---

# HD-32 Intake

## 1. Source Contract（读回事实，非转述）

- Issue HD-32 `f3e0e5ef-0e5f-4006-a4e9-b6fb603a821d`，Project「开源协作」。
- 上游 PR：`iflytek/skillhub#735`，OPEN / Draft / CONFLICTING。
- PR #735 公开 head：`e071afb4f0c1b124d484c9d1098b83924f0cfd1d`
  对应 fork 分支 `xiaoyaner0201/skillhub:20260819-skillhub-731-confirm-publish-event-publication-r1`
  （2026-08-24T02:40Z 由 `git branch -r --contains` 确认）。
- 该 head 与 upstream/main 的 merge-base 是 `d2403bb5911953b8f53e62c3f0a9edc291363944`，
  **不是**当前 main → PR 落后于 main，这是 CONFLICTING 的来源。
- 当前 upstream/main：`e8cab7389f5dc1084fdc1cdf70bafd314d0a3d50`（读回一致于 Issue 描述）。
- Maintainer XiaoSeS 裁定（comment 5368674127）四项范围，逐字：
  1. reject subscription creation when the current user cannot read the skill metadata;
  2. filter subscriber notifications using the current visibility/access rules at fanout time;
  3. reuse the existing `VisibilityChecker` or a shared access policy instead of introducing a parallel visibility model;
  4. add the missing i18n message and tests for private, namespace-only, hidden, removed-member, and disabled-user cases.
  并声明：不关闭 PR，但当前形态不可合并；drop 已由 #738 合入的 confirmPublish 变更；rebase 到最新 main；保持 draft。

## 2. 风险分级：HIGH_RISK（机械判定，不可自行降级）

命中的触发器族（`multica-engineering-delivery` §风险触发器）：

| 触发器族 | 本任务的命中点 |
|---|---|
| authentication / authorization / role / membership / **revocation** | 订阅创建授权；`removed-member`、`disabled-user` 场景 |
| PRIVATE / 跨 namespace | `private`、`namespace-only`、`hidden` 可见性场景 |
| event / notification / **fan-out** / subscriber / async listener | fanout 时按当时生效规则过滤订阅者 |
| 持久化派生记录 | 已存在的订阅行在权限收紧后成为陈旧派生记录 |

四族全中。**因此 Discovery Stage 为必经前置**：在派发 Plan 之前，必须由非 Planner 的独立 Run
产出 `behavior-inventory.v1`，其 `produced_by_run_id` / `produced_by_context_id` 必须 ≠ Plan Run，
否则 `run_gate.sh --kind plan` 直接 RETURN（自证）。

后续对 HIGH_RISK 的强制项（记录在此，届时由本 Leader 机械校验，不重复方法正本）：
QA 拆 blind Charter 与 execution 两 Run；Review 拆 `BLIND_BEHAVIOR_AUDIT` 与
`EVIDENCE_CHALLENGE` 两 Run；Council 由 Planner 在 Plan 中提名 2–3 具名 lens（含 ≥1 反对者）。

## 3. 分支与账本

- 内部交付分支：`20260824-hd32-skillhub-735-subscription-authz-hardening`
- 由 upstream/main `e8cab738` 直接开出（**不是**从 PR #735 head 开出，避免继承旧 base 与
  confirmPublish 变更；"drop 已合入部分"因此是构造性的，不需要 revert）。
- base tree `c81497cf7eb98f0568b8b30c9af1e67797bca27a`；base 中不存在 `multica-ledger/`。
- 账本骨架与 `index.md` 头部由本分录同时创建。

## 4. 历史候选分支：可检查输入，非可继承结论

`origin/20260821-skillhub-735-subscription-authz-hardening-r3`，head `6a61b74060bdd1666ce306718b27767eb223baff`，
merge-base 与 upstream/main **恰为 `e8cab738`**（即已基于当前 main）。

机械观察（`git diff --stat`，未做语义判断）：

- 业务面看起来与四项裁定同向：`SkillSubscriptionService`(+55)、新增
  `SubscriberAccessResolver`(+72)、`NotificationEventListener`(+18)、
  `VisibilityChecker`(+62)、`messages.properties` / `messages_zh.properties` 各 +1，
  以及大量测试。**不含** `SkillGovernanceService` / `SkillReviewSubmitService`
  （即 PR #735 head 中的 confirmPublish 面），与"drop 已合入部分"一致。
- 但同时改动了 `RbacService.java`(+39) 与 `NotificationService.java`(+12)。这两处是否落在
  XiaoSeS 四项范围内，**是语义问题，由 Planner 判定并在 Plan 中逐项 disposition**，
  Leader 不裁决。此处仅登记为待解释的范围外沿。

**硬约束（Issue 明文）**：该分支携带的 `multica-ledger/**`（plan-gate-r3、qa-gate-receipt-r1、
blind-behavior-audit-pass1 等）属于**已删除任务**的产物，**一律不得作为本 Issue 的 Gate 依据**。
本 Issue 的 inventory / Plan / QA / Review 必须全部重新产出。代码可读、可复用；结论不可继承。

## 5. 与 Issue 明文边界的一处张力（登记，不裁决）

Issue「业务与协作边界」写：不改变 `SubscriptionMetadataAccessPolicy` 既有授权语义，
尤其不改变 `PRIVATE -> owner || manager` 的结论。

而上游 review（FenjuFu，2026-08-20）在同一 PR 上给出的读法是：仓库 core semantic rules
文档的规范模型为 `PRIVATE -> owner only, not marketplace-visible`。

二者可能指向不同结论。**Issue 是 Source Contract 的一部分，其明文边界优先**；Planner 若在
建模中发现该边界与 Repository Invariants 实质冲突，应报 `INVARIANT_VIOLATION` 或
`PLAN_GAP` 并回到本 Leader 走路由，**不得自行选一边实现**。

## 6. Publication 边界（提前登记，避免终局才发现）

工程链终点是 exact subject tree = READY。Issue 期望结果中的「PR #735 基于最新 main」
属于 Publication，由主控会话承担，**工程 Agent 不执行**。

需提前知会的事实：PR #735 的 head 分支基于旧 main 且包含已合入的 confirmPublish 提交，
要把它变成「基于最新 main 的收窄变更」，Publication 时只有两条路径：
(a) force-push 该公开分支 —— Project description 明确列为**必须升级 老板** 的不可逆动作；
(b) 在该分支上 merge 最新 main 并把 confirmPublish 面解析回 main 版本 —— 不需要 force-push，
但历史较脏。
路径选择不是本 Squad 的判断，仅在此登记以便 READY 交接时一并提出。

## 7. 工具链观察

- 本 Leader 容器：无 `java`、无 `mvn`；有 `uv`（故 `uvx semgrep` 可用 → D2 可达）。
- Gate bundle 已复制到 `/workspaces/HD-32/gates/`，`run_gate.sh` 冒烟通过。
  validator `validate_gate.py` version `0.1`,
  sha256 `a7100d86232e21df4f032d07ecbc7e055f2353020be0858230db131a47ca4892`。
- 仓库专属 D1 夹具存在：`gates/fixtures/skillhub-java/discover_java_event_paths.py`
  （本仓库适用；跨仓库不可调用）。
- **构建/测试工具链可用性必须在 Code 阶段之前确认**，否则 QA 无法产出执行证据。
  已要求 Discovery Run 顺带回报。

## 交接

- 状态：INTAKE 完成，HIGH_RISK，Discovery 前置未满足 → **暂不派 Plan**
- 产物：分支 `20260824-hd32-skillhub-735-subscription-authz-hardening`，base `e8cab738`
- 下一步：`BEHAVIOR_DISCOVERY` 独立 Run → 验证（非 Planner，满足 validator provenance 约束）
- 未解决：§5 的授权语义张力；§6 的 Publication 路径选择；§7 的 Java 工具链可用性
- 需要决策：无（均在后续阶段按路由处理）
