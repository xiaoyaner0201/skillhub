---
ts: 20260821-172100
agent: qa
action: QA_EXECUTION
tree: 922195602c92f0f420e17df9021ae39727c95f8d
verdict: RETURN
---

本分录记录独立 `QA_EXECUTION` Run
`c78f0d9e-4324-4b2e-a41f-6ecc5607145f`。验证对象为业务 commit
`046c04ed6b5631bae7b854ae60797b3ed7a3a930` / exact tree
`922195602c92f0f420e17df9021ae39727c95f8d`；当前 ledger head 相对该 commit 的非
`multica-ledger/**` diff 为空。执行范围取 frozen Charter 18 组、Plan 28 probes 与既有回归
的并集。QA 未修改候选业务代码或候选测试代码。

## 结论

- `PLAN_COMPLIANCE: UNVERIFIED`
- `SYSTEM_BEHAVIOR: UNVERIFIED`
- `Overall: RETURN`（machine artifact 的 `overall` 为 `UNVERIFIED`）

Gate bundle 的结构/闭包 validator receipt 为 `PASS`，仅表示 artifact schema、inventory/Plan
digest、coverage set 与 Run 分离校验通过；它不是行为 PASS。artifact 自身登记 25 个
`BLOCKED`、8 项 blocking `UNVERIFIED` 与 5 个开放 HIGH/BLOCKER finding，因此不得结案。

## 动态证据

### exact-tree 全 reactor（QAC-16）

- 命令：在 `server/` 使用 repo-local JDK 21 / Maven 3.9.13 执行 `mvn test`；ledger-only
  head 的业务面机械绑定 exact tree `92219560`。
- 原始输出：`qa/raw/qa-execution-full-reactor.log`，SHA-256
  `037f271c3768624e75b84208d1ca98076e42f173696968836b7bc28e82d58e2b`。
- 模块汇总：21 + 417 + 126 + 35 + 10 + 35 + 768 = **1,412 tests**；0 failure、0
  error、1 skipped；`BUILD SUCCESS`，完成于 `2026-08-21T17:02:04Z`。

### targeted union 与 candidate control

- `qa/raw/qa-execution-targeted-union.log`（SHA-256
  `b50ecc85bb03be7d9b31ec0ae8cb5763122c15e1ad6c5015f3681c043613b694`）：64 tests，0
  failure/error/skipped，`BUILD SUCCESS`。涵盖 Plan tests、checker/8-caller suites、resolver、
  listener、dispatcher 与 Charter 相关回归。
- `qa/raw/qa-execution-red-candidate-control.log`（SHA-256
  `9ba7ea75bb5743e7688d75ed879e94de1b7c02e5cf234d258c70120892ff2411`）：14 个
  `expected_before=FAIL` probe 在候选上全部通过。

上述绿色证明 exact-tree build 与被执行的 unit/call-level behavior；不能覆盖未运行的真实
Spring proxy/transaction/executor/final-sink 信任边界。

## PLAN_COMPLIANCE

Plan 的 28 个 probe ID 已全部进入 artifact，但 QA 不把“测试方法 exit 0”直接提升为 probe
PASS：

- `subscribe-message-en-zh`、`existing-consumer-regression` 与 direct-syntax
  `dynamic-surface-delta-audit` 的既定可执行断言通过。
- subscribe / published / yanked 测试主要使用 mocked repositories、`NotificationService`
  与 `SseEmitterManager`；它们证明 checker、listener、dispatcher 的调用级拒绝/允许与 payload，
  不证明真实 subscription/notification row、SSE transport 或 producer→listener 路径。
- `published-batch-failure` / `yanked-batch-failure` 只注入 account repository failure；没有覆盖
  membership、platform role、namespace 四类 failure variants，也没有断言真实最终 sink。
- R2 正向顺序仅在 `SubscriberAccessResolverTest` 用 `containsExactlyElementsOf` 证明 resolver
  输出顺序；listener sink test 对两 recipient 使用普通 Mockito `verify`，未用 `InOrder`，故“最终
  recipient 顺序逐元素一致”仍未证明。
- `SubscriberNotificationRuntimeIntegrationTest` 只反射 annotation、直调 listener 或使用手写
  rejecting `Executor`；三个 runtime probe 均不满足 Plan/Charter 的真实集成判据。

故 `PLAN_COMPLIANCE=UNVERIFIED`。

## SYSTEM_BEHAVIOR

### Blocking runtime blind spots（QAC-09/10/11）

1. 未运行真实 `ApplicationEventPublisher → transaction commit →
   @TransactionalEventListener proxy → @Async(skillhubEventExecutor) → Notification DB/SSE`。
2. 未运行 real rollback 与 no-active-transaction 的零 row/零 SSE。
3. production `AsyncConfig.java:32` 配置 `CallerRunsPolicy`；测试中的手写 rejecting lambda 与
   configured executor 行为相反，未观察饱和时 handler thread、事务上下文、eligibility 或 sink。

### RED 独立复核（QAC-15）

- QA 逐项审读实现者保留的 RED 原始输出：14/14 是目标缺陷断言，0 errors；其中 9 项是 denied
  recipient 实际调用 `NotificationService.create`，1 项是 message code 裸回落，4 项是缺失应抛的
  `DomainForbiddenException`，不是环境噪声或编译错误。
- 但该 RED 对应的中间 source tree 没有可达 commit。QA 在 frozen base `e3312ae5` 应用最终
  candidate test diff 后重跑，因 final tests 引用了新增 `SubscriberAccessResolver`、full-context
  overload 与 batch repository API，产生 15 个 testCompile errors，未进入 14 probe。
- 原始复现：`qa/raw/qa-execution-red-base.log`（SHA-256
  `7f259284c0c752b0fbb2a8d0b72c27b40d1f32d8da37fc6b5064f46af5df3ef9`）。因此能确认旧日志
  failure 原因，却不能把它绑定到 QA 可审计/可复现 exact old tree，保留 `EVIDENCE_GAP`。

### 静态与动态完整性（QAC-18）

- fresh Semgrep 1.132.0：9 rules、511 production Java files、51 findings、0 parse errors；相对
  frozen discovery 的规范化 direct-syntax delta 仅新增计划内两个 `canAccess` caller：
  `SkillSubscriptionService.java:63` 与 `SubscriberAccessResolver.java:57`。
- 输出：`qa/raw/qa-execution-semgrep.json`，SHA-256
  `507cfe48b305e9c3068add41bb48e2f7530d1551f698b7b7323f4b7591c3fa20`。
- AST/Semgrep 不能证明 generated sources、reflection、external modules、classpath-discovered
  listeners、Spring proxy 或 runtime-conditional registration 完整，故该面仍 blocking
  `UNVERIFIED`。

因此 `SYSTEM_BEHAVIOR=UNVERIFIED`，不允许 Overall PASS。

## Gate artifact

- artifact：`qa/qa-gate-r1.json`，SHA-256
  `1f16d21a5432b18bc89eb143b731a5a072f032a2809dd803e9c260310c668cd2`。
- receipt：`qa/qa-gate-receipt-r1.json`，result `PASS`（结构 gate），SHA-256
  `f5dc5297671ef2656a63aa7bea2f2b1e09f9e7fe506ba546b46ad4b18e56aeed`。
- receipt 绑定 inventory `efd911ad…`、Plan artifact `39510ad8…`、subject commit
  `046c04ed`、tree `92219560`，validator `a7100d86…`。

## 必须进入 PR 正文的残留风险原句

> This change has not been validated through a real Spring transaction → transactional listener
> proxy → configured async executor → persisted notification/SSE integration path. Commit,
> rollback/no-transaction, and configured `CallerRunsPolicy` saturation behavior remain unverified;
> existing tests use direct listener calls, mocks, annotation reflection, and a synthetic rejecting
> executor.

> Static Java/Semgrep enumeration found no additional direct candidate edges, but generated sources,
> reflection, external modules, runtime conditional registration, and classpath-discovered consumers
> were not proven complete and remain a residual verification blind spot.

## 交接

- 状态：QA execution 已完成但 **RETURN / UNVERIFIED**；不得将 receipt `PASS` 或 1,412 绿测
  写成 QA Overall PASS。
- 产物：artifact、receipt、本分录与 5 份 raw output 均在 `qa/`，绑定 exact tree
  `922195602c92f0f420e17df9021ae39727c95f8d`。
- 下一步：Leader 按 finding 路由；若补 probe，必须由 Coder 修改候选测试/代码并冻结新 tree，
  然后重新执行 QA。Reviewer Pass 2 应挑战本 artifact 的 mock/final-sink 与 RED provenance。
- 未解决：real commit/rollback/no-transaction/configured-executor、final persistence/SSE、R2 listener
  顺序、四类 batch failure、reproducible RED tree、base-vs-candidate differential、
  generated/reflection/external completeness。
- 需要决策：无。Publication 边界未进入。
