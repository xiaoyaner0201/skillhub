---
ts: 20260821-155700
agent: coder
action: RED→GREEN
tree: 922195602c92f0f420e17df9021ae39727c95f8d
verdict: —
---

按冻结 `plan-r1` 实现订阅创建读权门禁、发布/撤回 fan-out 的事件时读权过滤，以及 full-context `VisibilityChecker`。候选业务 commit 为 `046c04ed6b5631bae7b854ae60797b3ed7a3a930`，subject tree 为 `922195602c92f0f420e17df9021ae39727c95f8d`。

## 实现边界

- `VisibilityChecker` 仅新增 full-context overload；两个既有签名及 8 个既有 consumer 均未迁移。
- `subscribe` 在 row、counter、event 之前装配当前 account / namespace / membership / platform role 并调用 checker；`unsubscribe` 与 `isSubscribed` 语义未改。
- subscriber fan-out 先读取当前 skill、namespace 与候选订阅者，再经 resolver 做三次批量取数并调用 checker，随后才进入 actor skip 与 `NotificationDispatcher`。
- 非 ledger 变更为冻结 write set 内 17 个实际文件（第 18 个 `NotificationDispatcherTest` 无需修改），`17 files changed, 900 insertions(+), 9 deletions(-)`；未触及 `confirmPublish`、`SkillReviewSubmitService`、migration/prune、workflow 或存量订阅清理。

## RED→GREEN 证据

- RED 原始输出：`coder/20260821-141300-red-output.log`。未实现 tree 上 14 个 `expected_before=FAIL` probe 全部按目标缺陷失败：`Tests run: 14, Failures: 14, Errors: 0, Skipped: 0`，`BUILD FAILURE`。构成为 subscription sink 9、i18n 1、subscribe 4。
- 初始 GREEN：`coder/20260821-143400-green-output.log`。计划定向集合 `Tests run: 54, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。
- 完整 reactor：`coder/20260821-143800-full-test-output.log`。各模块汇总共 1,412 tests，0 failure、0 error、1 skipped；末模块 `Tests run: 768, Failures: 0, Errors: 0, Skipped: 1`，`BUILD SUCCESS`，总用时 `30:04 min`。
- 最终候选定向复跑：`coder/20260821-155500-candidate-green-output.log`。在 sink probe 改为真实 resolver + checker 后，`Tests run: 54, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。
- 真实 resolver/checker → listener → dispatcher → notification row/SSE sink 复跑：`coder/20260821-155300-sink-green-output.log`，`Tests run: 16, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。
- 8 个既有 consumer 回归已包含于完整 reactor：domain 相关 92 tests、app 相关 10 tests，0 failure/error。
- R2 正向不误伤同时由 `SubscriberAccessResolverTest.readableRecipients_arePreservedElementForElementInCandidateOrder` 的逐元素/顺序断言，以及真实 listener/dispatcher sink 的 `publish_readableRecipients_remainElementForElementAfterFiltering` 覆盖。
- 五类场景（private、namespace-only、hidden、removed-member、disabled-user）× 三条路径（subscribe、publish、yank）的 15 个独立具名断言均存在并已执行。
- EN/zh 均由 `ResourceBundleMessageSource` 从两个 properties 解析为真实文案，并断言不回落 message code。

## 静态发现与边界审计

- 候选 Semgrep 输出：`coder/20260821-150900-semgrep.json`；9 rules 扫描 511 production Java files，0 parse/error，51 findings。相对冻结 discovery 的规范化 delta 仅新增两个计划内 `VisibilityChecker.canAccess` 调用：`SkillSubscriptionService` 与 `SubscriberAccessResolver`；producer、listener、dispatcher、persistence/SSE sink 集合未新增。
- `git diff --check` 通过；变更路径全集是冻结 write set 子集；禁止路径/新增禁止符号扫描为空。

## `SubscriberAccessResolver` 逐行证伪口径

审查范围为 `SubscriberAccessResolver.java:43-64`：

- 43：仅按输入顺序去重 candidate IDs。
- 44–48：恰好两类批量事实读取并建 account / membership map。
- 49–53：第三类 platform-role 批量读取，仅把 binding 转成 code set；不比较任何 role code。
- 54–63：逐 candidate 取 map 中事实、把 membership role 作为参数传给 full-context checker，并记录 checker 的 boolean。
- 64：只按 checker boolean 过滤，保持 candidate 顺序。

这段没有 visibility、hidden、latest-null、namespace archived、account active、role allow/deny、event type、actor/publisher 或 `wasPublished` 判断，也没有在 checker 结果之后二次放行/拒绝。唯一的 `membership == null` 三元表达式只是把缺失事实转成 checker 的 `null` 参数，不作授权决定。

## 明确保留的 UNVERIFIED

`SubscriberNotificationRuntimeIntegrationTest` 只刻画源码注解 phase 与确定性的 pre-sink executor rejection，不冒充真实 Spring transaction/proxy/executor 集成。因此以下仍为 `UNVERIFIED`，必须由 QA/Review 保留并独立验证：

- real commit 是否经 Spring proxy/真实 executor 到达 async listener 与 sink；
- real rollback 是否产生零 sink；
- real configured executor rejection 是否产生零 sink；
- generated/reflection/external runtime surface 是否完整。

Semgrep 对冻结规则匹配面给出 delta 证据，但不得把 0 errors 或 inventory 未列出解释为上述动态面不存在。

## 交接

- 状态：候选业务 tree 已冻结；Coder 不自签 QA/Review PASS。
- 产物：business commit `046c04ed6b5631bae7b854ae60797b3ed7a3a930`，subject tree `922195602c92f0f420e17df9021ae39727c95f8d`。
- 下一步：QA 以 independent charter ∪ Plan probes ∪ regression 验证同一 exact tree；Reviewer 按上述逐行口径独立证伪 resolver。
- 未解决：四项真实 runtime/dynamic completeness 仍为 `UNVERIFIED`。
- 需要决策：无；Publication 边界未进入。
