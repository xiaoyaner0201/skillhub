---
ts: 20260823-134500
agent: coder
action: RED
tree: 57a240252dabc45b9f4dcc4cfa3500b5eca0e205
verdict: —
---

HD-31（第 11 次派单，全新 workdir `6e3a1644`）。本分录只做 RED，不做 GREEN、不做重构。

**lane 归因（本分录起生效）**：本轮由 `实现` `fd2d1252` 的 Claude Opus lane 执行
（`claude/claude-opus-5`，runtime `56fd0920`，thinking `high`，`--effort high`），
非此前的 `codex/gpt-5.6-sol-xhigh` / runtime `49238814`。这是一次**显式 lane 变更**，
不是等价替换：`ab_hard_constraint` 所保护的 A/B 对照面（R-P1-02 / R-P1-07）自本轮起
**在 `实现` 侧不再成立**，后续任何引用本分录证据的结论都必须显式标注 lane。
`规划` `e9cf031c` 与 `验证` `4f83bacc` 仍在 codex，Squad 未塌缩为单 lane，
Council runtime 异构保留。

## 一、提交面：零内容 merge 已被真实内容 commit 取代

| | 值 |
|---|---|
| 分支 | `20260821-skillhub-735-subscription-authz-hardening-r3` |
| 前 head | `3da0af1c` |
| **subject commit** | **`45b723ce7612ae69bb89fd51022443176e0e077e`** |
| **subject tree** | **`57a240252dabc45b9f4dcc4cfa3500b5eca0e205`** |
| author / committer | `xiaoyaner-multica-coder[bot]`（双方均是，无人工署名残留） |
| `git diff 3da0af1c HEAD` | **6 files / +1075 / −35**（非空） |
| worktree | clean（`git status --porcelain -uall` 空） |

落地形状取 brief「**RED delta 直接落在 …-r3**」的第一支：delta 是 r3 上的一条
普通内容 commit，**不是 merge**，因此不存在复制 `ddb9560b` 侧分支形状的可能。
`ddb9560b`（树与 `a17eab1d` 逐字节相同的零内容 merge）不被引用、不被续接。

### 内容出处机械可验

抢救分支 `origin/20260822-hd30-red-rescue-session-01a02a28` head `f90d64ee` 由
千乘妍以 `chore(rescue)` 人工署名提交，缺的正是 coder 提交面。本轮把它重做为
coder 署名，并在提交前机械核验内容同一性：

```
git diff --cached                     >  staged.patch
git diff 3da0af1c f90d64ee -- server/ >  ref.patch
cmp staged.patch ref.patch            ->  IDENTICAL
```

因此 brief 的**免读写集充分条件**成立：无一路径超出 `f90d64ee`，
无一字节偏离 `f90d64ee`。断言槽位与 preference 闸门照 `f90d64ee` 现状**未动**。

落地 6 文件（全部在 `src/test/` 下，无生产代码）：

```
M  server/skillhub-app/src/test/java/.../listener/SubscriberAccessResolverTest.java              +73
M  server/skillhub-app/src/test/java/.../listener/SubscriberNotificationRuntimeIntegrationTest.java +653
M  server/skillhub-app/src/test/java/.../listener/SubscriberNotificationSinkTest.java             +8
A  server/skillhub-app/src/test/java/.../notification/sse/RecordingSseEmitterTestConfiguration.java +219
A  server/skillhub-auth/src/test/java/.../auth/rbac/RbacServiceTest.java                          +71
A  server/skillhub-domain/src/test/java/.../domain/skill/VisibilityCheckerLegacyCompatibilityTest.java +86
```

## 二、上游基线 RED 面：非空 diff，非零内容 merge

`agent/hd30-red-base-20260822` 在 origin 上已不存在，无从续接。本轮**不重建侧分支
形状**，只把已有的 coder 署名 commit `9f3e5858` 命名为可寻址 ref：

```
git rev-parse agent/hd30-red-base-20260823^   ->  e8cab738…   (== 冻结 base)
git diff --shortstat e8cab738 agent/hd30-red-base-20260823
                                              ->  4 files / +485 / −6   (非空)
```

`9f3e5858` 的 parent 本就**恰好是** `e8cab738`，故「从 `e8cab738` 新建分支并
cherry-pick `9f3e5858`」的结果与 `9f3e5858` 本身逐字节同树同父。重新 cherry-pick
只会造出一条内容重复的 commit，故未做——机械等价，且避免伪造提交历史。
该分支**只作 RED 取证用，不并入 r3**，r3 侧的 delta 已由 §一 的真实内容 commit 承担。

已推 fork：`origin/agent/hd30-red-base-20260823` = `9f3e5858`。

## 三、RED 实跑（三次，全部为真实执行，无一项由推断填写）

环境：Temurin 21.0.12.1+1（aarch64），maven wrapper 3.9.13，warm 本地仓库 125M。

> **环境侧偏差，需登记**：`$HOME` 是 `noexec` tmpfs（`findmnt /home/agent` →
> `nosuid,nodev,noexec`），而 `mvnw` 要 exec `$MAVEN_USER_HOME/wrapper/dists/…/bin/mvn`，
> 首次调用 **exit 126 / Permission denied**（与 12:57 那次同因）。处置：把
> `MAVEN_USER_HOME` 指向持久卷上的同一份 wrapper dist + 同一份 125M 仓库
> （`/workspaces/…/.toolchains/m2`）。**只改环境变量，mvnw 命令行与 brief 逐字相同。**
> 该失败与恢复都保留在 `20260823-132753-red-output.log` 原始输出里，未抹除。

### Run A — brief 原命令，r3 + delta

`coder/20260823-132753-red-output.log` · `EXIT=1` · **BUILD FAILURE**

```
skillhub-domain ... SUCCESS   VisibilityCheckerTest 29/0  VisibilityCheckerLegacyCompatibilityTest 1/0
skillhub-auth ..... FAILURE   RbacServiceTest 1 run / 1 FAILURE
skillhub-notification / infra / search / app ... SKIPPED
```

RED 成立。但 reactor 在 `skillhub-auth` 即停，**12 个目标类里有 7 个根本没跑**——
以此单次输出下结论会高估覆盖面。故补 Run B。

### Run B — 同 12 类、同模块、r3 + delta，加 `-Dmaven.test.failure.ignore=true`

`coder/20260823-132947-red-output.log` · `EXIT=0`（reactor 不再中断）· 断言未改

**RED = 9 个失败 / 4 个类**：

| 类 | 失败/总数 | 失败点 |
|---|---|---|
| `RbacServiceTest` | 1/1 | `getUserRoleCodesByUserIds` 尚不存在 → `Expecting actual not to be null` |
| `SubscriberAccessResolverTest` | 1/5 | 生产侧传 `[]`，测试要求兜底 `[USER]`（`SubscriberAccessResolver.java:57`） |
| `SubscriberNotificationRuntimeIntegrationTest` | 6/15 | 收件人顺序 2 条、平台角色失败时最终 sink 非空 2 条、饱和 executor 超时 2 条 |
| `SubscriberNotificationSinkTest` | 1/12 | `yank_lastPublishedVersion_publicNonActorGetsExactRevocationSink` |

同批 GREEN（回归未被打破）：`VisibilityCheckerTest` 29、
`VisibilityCheckerLegacyCompatibilityTest` 1、`NotificationDispatcherTest` 3、
`NotificationServiceTest` 10、`SkillSubscriptionControllerTest` 7、
`SubscriptionMessageBundleTest` 1、`NotificationEventListenerTest` 14、
`SkillSubscriptionServiceTest` 13。

**探针贴的是真实生产入口与最终 sink，不是孤立 helper**：失败回溯落在
`NotificationEventListener.lambda$onSkillPublishedForSubscribers$1(:93)`、
`NotificationEventListener.lambda$onSkillVersionYankedForSubscribers$2(:120)`、
`NotificationDispatcher.dispatch(:40)`、`SubscriberAccessResolver.resolveReadableSubscribers(:57)`，
断言对象是 `notificationDispatcher.dispatch(...)` / `notificationService.create(...)` 的
真实收件人、payload 与调用顺序，以及真实 SSE runtime 上的持久化行。

### Run C — 上游基线敏感性，`e8cab738` + `9f3e5858`（纯净上游生产代码）

`coder/20260823-133600-red-output.log` · `EXIT=0` · 独立 worktree，独立 target

**RED = 14 个失败 / 3 个类**，逐条对应维护者四条要求：

| 类 | 失败/总数 | 覆盖 |
|---|---|---|
| `SkillSubscriptionServiceTest` | 4/13 | subscribe 侧 private / namespace-only 已移除成员 / hidden / disabled-user，均断言**变更前拒绝**（`assertForbiddenWithoutMutation`） |
| `SubscriptionMessageBundleTest` | 1/1 | 缺失 i18n key（中英双语解析） |
| `SubscriberNotificationSinkTest` | 9/9 | fanout 侧同五类 + `yank_lastPublishedVersion_nonOwnerGetsNoSink`，断言**零行零 SSE** |

**一条必须如实说明的例外**：`VisibilityCheckerLegacyCompatibilityTest` 在上游基线
**1/1 全绿**（`20260823-133600-red-output.log:95`），在 r3 上同样全绿。它按设计是
**既有行为的回归护栏**（15 条 `HD30_LEGACY_MATRIX` 断言原样锁住上游可见性语义），
**不是 RED 探针**，不具备也不应具备 RED 敏感性。不要把它计入 RED 证据。

## 四、边界与未做项

- 未写任何生产代码，未做 GREEN，未重构。9 + 14 条失败**保持红**，交由后续 GREEN 阶段。
- 未改 Plan、未扩权、未动 non-goals、未碰 PR #735、未转 Ready、未 force-push、
  未 push 任何公开分支（两次 push 均为自有 fork 的内部分支，且均为 fast-forward）。
- 未自签 QA / Review PASS。`green_dispatch_gate` 的 (a)(b)(c) 由调度实跑复核。
- 未发现 `PLAN_GAP`：源码、需求与系统不变量与 brief 无冲突。
- 未发现 `RISK_ROUTE_GAP`：本任务已标 `HIGH_RISK`，与命中的 authz / PRIVATE /
  event-fanout / async 事务四类触发器相符。
- 12:57 与 13:01 两份上一轮（中毒 session）遗留 log 留在磁盘上未提交、未覆盖、未删除；
  r1 的三份旧 log 同样未复用、未覆盖。本轮三份均为新文件。

## 交接

- 状态：**RED 成立并已实跑留痕**。index 头部三条不受理理由（零内容 merge / probe 只存在于
  人工署名的 `f90d64ee` / 无 RED 实跑输出进入可读 ref）本轮全部关闭。
- 候选 exact tree：**`57a240252dabc45b9f4dcc4cfa3500b5eca0e205`**（commit `45b723ce`），
  worktree clean，已推 `origin/20260821-skillhub-735-subscription-authz-hardening-r3`。
- 取证 ref：`origin/agent/hd30-red-base-20260823` = `9f3e5858`，parent `e8cab738`。
- 原始输出：`coder/20260823-132753-red-output.log`、`coder/20260823-132947-red-output.log`、
  `coder/20260823-133600-red-output.log`（`.gitignore:19` 为 `*.log`，三份均以 `git add -f` 入库）。
- 下一步：调度实跑复核 `green_dispatch_gate` (a)(b)(c)；通过后派 GREEN。
- 未解决：`SubscriberNotificationRuntimeIntegrationTest` 两条饱和 executor 用例以
  **超时**形式失败（`Timed out waiting for blocked SSE notification`），而非断言失败。
  超时类 RED 对环境负载敏感，GREEN 阶段需确认其转绿是实现所致而非机器更闲所致。
- 需要决策：无。lane 变更归因见本分录开头，账本侧代价由 leader 在头部登记。
