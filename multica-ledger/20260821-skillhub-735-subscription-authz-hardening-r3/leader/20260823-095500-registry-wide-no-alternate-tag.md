# leader / 20260823-095500 / 检查点未达成（独立复读）；**全 registry 范围的否定结论：不存在可替代的 tag**

- Agent：leader（调度，`96798779-3720-43f7-9ee1-a8389ad7ab0e`）
- 触发评论：`ad2d01e6-2eed-4d08-abb2-a1166a735802`（2026-08-23T09:50Z，dongsjoa 巡检）
- Stage：`PLAN_APPROVED_R2` 不变；Issue 保持 `blocked`；**不重派、不重试、不改配置、不碰镜像/compose/容器**
- 分录性质：检查点复读 + **否定结论登记（关闭两条尚未被任何一方定价的路径）**
- **不含任何 RED 受理结论**；`20260822-174500` 的裁定一字不动

## 一、检查点独立复读：**未达成**，与巡检一致

`hub.docker.com/v2/.../tags/next` 直读：`last_updated` = `2026-08-22T23:37:24.404383Z`、
digest = `sha256:6f95064b…`，与 leader 09:10Z 与巡检 09:47Z 三次读数**逐字相同**。

## 二、**新登记的否定结论：整个 registry 里没有任何一个 tag 含该修复**

巡检只查了 `next` 与 `next-web` 两个 tag。leader 本轮按 `ordering=last_updated` 降序拉取
`diegosouzapw/omniroute` 的完整 tag 列表（`count` = **287**），头部实测：

| tag | last_updated | digest |
|---|---|---|
| `next-web` | **2026-08-22T23:37:26.664804Z** | `sha256:53b69fae…` |
| `next` | 2026-08-22T23:37:24.404383Z | `sha256:6f95064b…` |
| `main-web` | 2026-08-22T01:27:29.104327Z | `sha256:5b5140a5…` |
| `main` | 2026-08-22T01:27:26.851066Z | `sha256:a30676da…` |
| `latest-web` / `3.8.49-web` | 2026-07-30T00:59:3{7,6}Z | `sha256:a3155c41…` |
| `latest` / `3.8.49` | 2026-07-30T00:59:3{3,2}Z | `sha256:92c768c5…` |

**全库最新的 tag 是 `next-web`，时间戳 `2026-08-22T23:37:26.664804Z`。**
即自 23:37:26Z 起，**该 registry 内没有任何一个 tag 被发布或更新过**——不只是 `next` 冻结，
是整个 tag 命名空间冻结。

**该结论对截断免疫**：返回页在 `3.8.24` 处被截断（共 287 个 tag，未全部读到），
但列表按 `last_updated` **降序**，截断只可能隐藏**更旧**的 tag，不可能隐藏比 `next-web`
更新的 tag。故「无任何 tag 晚于 23:37:26Z」成立。

**⇒ 「换一个 tag 拉」这条路关闭。** 不是"`next` 恰好卡住、别的通道还活着"，是全部通道同时停摆。

## 三、同一份数据给出的三条附带事实

### 3.1 `-bun` / `-web-bun` tag **一次都没成功发布过**

`de5e237a`（`feat(ci): publish Bun container images`）提交于 08-22T23:31:59Z。若这两个 tag 曾发布，
其 `last_updated` 必晚于该时刻，在降序列表头部一定可见——**实际不存在**。
这是从 **registry 侧**（而非 Actions 侧）独立佐证了巡检 §二的归因：
新增的 BUN 发布面自引入起产出为零，与「双架构在 `Build and push platform image by digest` 同时挂」相容。
（Actions 日志正文需 token，leader 与巡检同样无凭据，**双方都不宣称已读**。）

### 3.2 registry 侧独立复证了 `20260823-091000` §二的双通道模型

`resolve-docker-publish-version.sh` 写的是 `main → VERSION="main"`、
`release/v*`（且为默认分支）`→ VERSION="next"`。registry 里 `main` 与 `next` **两个 tag 并存且时间戳不同**
（08-22T01:27 vs 08-22T23:37），与该脚本的两个分支逐一对应。
这是与 git 完全无关的第三个证据面，前一分录的通道结论不再依赖单一来源。

### 3.3 回滚目标真实存在，但**回滚不是修复路径**

快照 `docker-compose.yml.pre-3.8.50-20260822-215737.snapshot` 钉的
`3.8.49@sha256:92c768c5…` 在 registry 中实存，digest 逐字匹配（`latest` 同 digest）。
即回滚可执行。但该镜像发布于 **2026-07-30**，早于修复近一个月——
**回滚既不含 `e73ab004`，也不含此后任何改动，对本 Issue 的一阶故障面无任何作用。**
登记此条，是为避免把"有可回滚的锚点"误读为"有可用的退路"。

## 四、另一条一并排除的路径：自行构建镜像

理论上可从含修复的源码自行 build 镜像替换。**leader 不列为候选**：
它与"自行打补丁"落在同一授权边界内（生产 Live 变更、跨 workspace），且爆破半径**严格更大**
（整镜像 vs 单文件），回收时的静默回归面亦同（`20260823-091000` §五第 3 点）。
登记为**成本上严格劣于已撤回的自打补丁支**，不需要单独定价。

## 五、裁决面：选项集不变，一条量化前提再次恶化

leader 同意巡检 §四：等待期望时间**不可估**。补充一条 leader 侧可量化的方向性事实——
`e73ab004` 与 release head 的距离已从 `20260823-091000` 实测的 **0/22** 继续拉开（巡检记 head 已换 3 次至 `35a34ac3`），
即**修复代码与本机可用镜像之间的差距在单调变大**：即便 CI 恢复，届时那次成功构建将携带的是
一个比现在远得多的树，其变更面未经我方任何验证。这不构成反对等待的理由，但意味着
"等镜像"并不等价于"只拿到那一个修复"。

`20260823-091000` §五登记的隐患（本地分叉回收成本随时间单调上升 + 镜像恢复后手改补丁静默消失）
权重上升，leader 同意。**是否维持撤回、以及 lane 路由，仍归 dongsjoa，leader 不代决、本轮不重复升级。**

## 六、动作边界与不变项

只读：Docker Hub v2 公开 tag 列表与单 tag 端点（匿名）。**无任何写操作**——未 push 镜像、
未改 compose、未拉镜像、未重启容器、未改任何连接/agent 配置、未重派、未重试、未动 Gate、
未签异构豁免、未代决路由、未去上游开 issue、未 @ 任何人。

`green_dispatch_gate` CLOSED（(a)(b)(c) 一字不改）、`red_intake` NOT ACCEPTED 三条理由、
`write_set_precheck` CLEAN、加绑条件 2 的唯一受理证据（addendum `3812652e`）、
Plan Gate artifact `61d54ad3…`、R1–R5 与 A/B 硬约束、Publication 边界，全部不动。
9 次失败仍归 runtime/infra，不计入 Coder、不进返修链。脏会话弃用仍是共同必要前置。
