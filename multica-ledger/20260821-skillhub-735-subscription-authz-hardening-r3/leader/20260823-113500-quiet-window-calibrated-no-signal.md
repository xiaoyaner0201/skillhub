# leader / 20260823-113500 / 给"上游推流停了"这条观察做量化标定：**它落在该分支的常态区间，不携带信息**

- Agent：leader（调度，`96798779-3720-43f7-9ee1-a8389ad7ab0e`）
- 触发评论：`2adbaddf-ccf4-45e3-8d4b-901f9f90e054`（2026-08-23T11:30Z，dongsjoa 巡检）
- Stage：`PLAN_APPROVED_R2` 不变；Issue 保持 `blocked`；**不重派、不重试、不改配置、不碰镜像/compose/容器**
- 分录性质：观察锚点标定（把一条"两种解释都相容"的观察定量化）
- **不含任何 RED 受理结论**；`20260822-174500` 的裁定一字不动

## 一、复核巡检本轮唯一的新事实：属实

leader 匿名 fetch 实测：`release/v3.8.50` head 仍为 **`0b41259f`**（`2026-08-23T07:01:11-03:00`
= 10:01:11Z），至本轮读取时（11:32Z）**静止 1 小时 31 分**，与巡检一致。

巡检的处置也正确：单点确实无法区分"有人在处理故障"与"上游进入静默期"，故**不作归因**。
leader 同意该处置，并按 `20260823-105000` §二登记的教训（**"与假说相容"不等于"支持该假说"**）
再进一步——**先量化这条观察本身有没有判别力，再决定它值不值得当锚点**。

## 二、标定：1.5 小时的静默在该分支上是**常态**

取 `release/v3.8.50` 自 2026-08-09 起的全部 commit（**1022 个**，1021 个间隔）：

| 统计量 | 值 |
|---|---|
| 间隔中位数 | **0.01 h**（≈36 秒，高频合流） |
| p90 / p95 / p99 | 0.52 h / **1.25 h** / 6.95 h |
| 最大间隔 | 25.48 h |
| **≥ 1.5 h 的间隔** | **46 个 / 1021（4.5%）** —— 两周内平均每天约 3 次 |
| ≥ 3 h | 25 个 |
| ≥ 6 h | 12 个（最长 25.5 h） |

**当前 1h31m 的静默大致落在 p95～p96，两周内已发生过 46 次。**
即：它既不异常、也不罕见，**对"CI 是否有人在修"没有任何判别力**。
把它当锚点会重演 `20260823-105000` §二那类错误——**在一个高频分支上，短时静默是背景噪声，不是信号**。

## 三、一条弱得多、但方向一致的旁证（明确标注其证据强度）

按上游本地时区（`-03:00`）折算：**2026-08-23 是星期日**，当前本地时间约 **08:32**。
样本内周日各小时的 commit 计数：

```
03:00→1   04:00→0   05:00→0   06:00→7   07:00→4   08:00→0   09:00→55   10:00→9
```

即当前正处在周日的历史最低谷，而活动通常在**本地 09:00（= 12:00Z）**骤然恢复。

**但这条证据很弱，必须标注**：样本内只有 3 个周日（08-09 / 08-16 / 08-23），其中今天还是不完整的，
实际只有 **2 个完整周日**支撑该小时分布。故它**只能作为方向性旁证**，不能单独承重。
本分录的结论由 §二（1021 个间隔的全样本统计）承担，§三仅为一致性说明。

## 四、由此得到一个**可证伪**的下轮预期（优于无标定的锚点）

若该分支行为正常，活动应在本地 09:00 ≈ **12:00Z** 前后恢复。据此：

- **静默持续到 12:00Z 之前** → 完全在常态内，**不构成任何新信息**，不应写入裁决面；
- **静默显著越过 12:00Z 仍无 commit** → 此时静默才开始偏离常态，**届时**它才值得作为观察项登记
  （但仍不足以归因，只是"值得看"）。

`20260823-105000` §六登记的那条纯 git 先行信号**不变、且仍是主判据**：
`docker-publish.yml`（baseline `de5e237a`）或 `Dockerfile`（baseline `11884013`）在 release 分支上
出现任何新 commit。**该信号与推流是否静默无关，不受本节影响。**

## 五、其余全部不变

检查点 ①②③ 与 `20260823-105000` §六一致，判据不动；`upstream_fix` 未达成；
`base_drift` 仍为零（本轮未重复核验 skillhub base，`105000` §五结论在无我方产出、
无上游 commit 的前提下继续成立）；等待仍是无主、无 ETA；隐患权重不因本轮变化。

**待裁决项一字不变**：lane 路由 vs 本地网关补丁，脏会话弃用为共同必要前置，归 dongsjoa。
本轮无新增可交付信息，**leader 不重复升级、不代决**。

## 六、动作边界与不变项

只读：匿名 fetch 上游 `release/v3.8.50`，读 commit 时间戳并做本地统计。
**无任何写操作**——未 push/pull 镜像、未改 compose、未重启容器、未改任何连接/agent 配置、
未重派、未重试、未动 Gate、未签异构豁免、未代决路由、未去上游开 issue、未 @ 任何人。

`green_dispatch_gate` CLOSED（(a)(b)(c) 一字不改）、`red_intake` NOT ACCEPTED 三条理由、
`write_set_precheck` CLEAN、加绑条件 2 的唯一受理证据（addendum `3812652e`）、
Plan Gate artifact `61d54ad3…`、R1–R5 与 A/B 硬约束、Publication 边界，全部不动。
9 次失败仍归 runtime/infra，不计入 Coder、不进返修链。
