# leader / 20260823-121000 / **自陈判据缺陷：leader 登记的「纯 git 先行信号」不是恢复的必要条件**，须降级；恢复主判据应为巡检已在读的 registry 观测

- Agent：leader（调度，`96798779-3720-43f7-9ee1-a8389ad7ab0e`）
- 触发评论：`f239cba4-be3a-4556-96be-ff6634bf6d87`（2026-08-23T12:05Z，dongsjoa 巡检）
- Stage：`PLAN_APPROVED_R2` 不变；Issue 保持 `blocked`；**不重派、不重试、不改配置、不碰镜像/compose/容器**
- 本分录 **修正**：`20260823-105000` §六第 3 点、`20260823-113500` §四末段对该信号的**地位表述**（信号本身不作废，降级）
- **不含任何 RED 受理结论**；`20260822-174500` 的裁定一字不动

## 一、检查点独立复读：未达成，与巡检一致

leader 匿名 fetch（12:06:28Z）：`release/v3.8.50` head 仍为 **`0b41259f`**
（`2026-08-23T07:01:11-03:00` = 10:01:11Z），静止 **2 小时 05 分**。

`20260823-113500` §四登记的可证伪预期（活动应在本地 09:00 ≈ 12:00Z 前后恢复）**其判定窗口刚刚打开**：
12:06Z 时超出 6 分钟，**该量级不构成偏离**，本轮不据此得出任何结论。
按 `113500` §二的标定，2h05m 落在 p96 附近，**仍在噪声带内**——巡检的处置正确，leader 无异议。

## 二、本轮实测到的一条正向 git 事实（此前只登记过"缺席"）

`20260823-105000` §四登记的是一条**缺席**证据（`docker-publish.yml` 无人改动）。本轮补一条**正向**的：
断裂窗口内上游确实有人在做「修红」，且其两次动作**都不含发布面**。

| commit | 时刻 | 距断裂起点 | 标题 | 触碰文件 |
|---|---|---|---|---|
| `79c5bdf68` | 08-23T02:09:16Z | +2h46m | `fix(release): repair v3.8.50 base-red tail after latest root lift` | `src/`、`open-sse/`、`tests/`、`docs/`、`scripts/build/` —— **无 `.github/`** |
| `2264cffe4` | 08-23T10:00:36Z | +10h37m | `fix(ci): **finish** greening release/v3.8.50 base` | `tests/unit/` × 5 —— **无 `.github/`** |

（断裂起点取 `805252a9` `2026-08-22T20:23:04-03:00` = 23:23:04Z，leader 已于 `105000` 用 git 独立核过。）

并附全量普查——`de5e237a..FETCH_HEAD` 共 **47 个 commit** 触碰的顶层目录计数：

```
91 src   88 tests   54 docs   39 open-sse   17 changelog.d   3 config
 1 scripts   1 public   1 package.json   1 package-lock.json   1 bin   ...
```

**`.github/` 出现 0 次。** 即不只是 `docker-publish.yml`，**整个 CI 配置面 47 个 commit 无人触碰**。

## 三、按 `105000` §二的教训自评这条证据的区分力：**弱**

诱惑在于把它读成"连自称在修 CI 的人都没管发布面 ⇒ 更坐实无人在修"。**leader 不这样登记**，理由：

1. `finish greening ... base` 里的 "base" 指的是该分支的**测试基线**（题注挂 `#9985`/`#11203`），
   不是整个 Actions 面；该 commit **从未声称** publish 是绿的。把它读成"宣称已绿却漏了发布"是过度解读。
2. 更要命的是 **`105000` §四已经论证过起因在两个构建定义之外**（边界两侧逐字节相同、输出却翻转），
   即大概率在 registry 侧（配额/鉴权/缓存）。**registry 侧的修复根本不会产生任何 git 痕迹。**
   因此"没有 `.github/` commit"在 H1（有人在修）与 H2（无人在修）下**都可能为真**——
   又是一条低区分力观测。按 `105000` §二，**不得当作支持性证据**。

故本节只作为**背景事实**登记，**不用于加强任何归因**。

## 四、但由此推出的这条后果是硬的：**leader 给的先行信号存在假阴性**

`20260823-105000` §六第 3 点把它写成"纯 git 的等价先行信号"，`113500` §四又称其为"主判据"，
巡检本轮据此写下「**判别信号依旧只有一个**」。**这个地位表述是错的，必须由 leader 自己更正。**

| | 该信号（新 commit 触碰 `docker-publish.yml` / `Dockerfile`） |
|---|---|
| 对"有人正在**以改仓库的方式**修复" | **充分**——触发即为真信号 |
| 对"发布通道**恢复**" | **不必要**——若起因在 registry 侧（§三.2，即 `105000` §四自己的推论），修复不改仓库，该信号**永不触发**，而 `next` 可能已经重新发布 |

**⇒ 把它当作等待的闸门，存在"实际已恢复而判据始终不响"的失效模式。**

**恢复的主判据必须换成巡检每轮已经在读的那一条**：`next` 的 `last_updated` / digest 发生变化。
它对任何成因都会触发，且它**就是我们真正需要的东西**（一个可拉取、含 `e73ab004` 的镜像），
不存在盲区。git 信号**降级为"触发即有效、不触发不说明任何事"的加分项**，可以继续观察，但不得承重。

两条判据回答的是**不同问题**：registry 观测答"恢复了吗"，git 观测答"有人在以改仓库的方式修吗"。
`105000` / `113500` 把后者写成前者的替代，是 leader 的表述缺陷，本分录更正。
**巡检本轮的读数方式本身没有问题——它一直在读 registry；需要改的是 leader 给它的判据措辞。**

## 五、不受影响的条目

`no_alternate_tag`、`no_config_workaround`、`failure_root_cause`、`base_drift` 零漂移与
"Gate 无时间维度"（`105000` §五）、`113500` §二的静默标定，**全部不受本分录影响**——
它们都不依赖该 git 信号的地位。检查点 ①（`next` 更新）与 ②（`docker-publish.yml` 出现
`conclusion=success`）**判据不变**；③ 的"纯 git 等价形式"按 §四降级。

待裁决项一字不变：lane 路由 vs 本地网关补丁，脏会话弃用为共同必要前置，归 dongsjoa。
本轮无新增可交付信息，**leader 不重复升级、不代决**。

## 六、动作边界与不变项

只读：匿名 fetch 上游 `release/v3.8.50`，读 commit 时间戳、`--stat` 与 `--name-only` 普查。
**无任何写操作**——未 push/pull 镜像、未改 compose、未重启容器、未改任何连接/agent 配置、
未重派、未重试、未动 Gate、未签异构豁免、未代决路由、未去上游开 issue、未 @ 任何人。

`green_dispatch_gate` CLOSED（(a)(b)(c) 一字不改）、`red_intake` NOT ACCEPTED 三条理由、
`write_set_precheck` CLEAN、加绑条件 2 的唯一受理证据（addendum `3812652e`）、
Plan Gate artifact `61d54ad3…`、R1–R5 与 A/B 硬约束、Publication 边界，全部不动。
9 次失败仍归 runtime/infra，不计入 Coder、不进返修链。
