---
ts: 20260821-113951
agent: leader
action: intake + risk grading + branch/ledger init + discovery dispatch
tree: c81497cf7eb98f0568b8b30c9af1e67797bca27a
verdict: —
---

HD-30 intake。本分录只做机械判定与编排事实登记，不含任何语义裁决。

## 1. 风险分级（机械）

按 `multica-engineering-delivery` 风险触发器逐条比对 Issue 需求面：

| 触发器 | 命中处 |
|---|---|
| authentication / authorization / role / membership / revocation | R1 订阅创建读权校验；R2 fanout eligibility；removed-member / disabled-user 场景 |
| PRIVATE、跨 namespace | R1/R2 的 private、namespace-only、hidden 可见性判定 |
| event / notification / fan-out / subscriber / async listener | R2 `NotificationEventListener#onSkillPublishedForSubscribers` / `#onSkillVersionYankedForSubscribers` |
| 并发、事务边界、陈旧状态 | R2 要求「基于事件处理时刻的实时状态，不得使用订阅创建时快照」 |

判定：**`HIGH_RISK`**。四类同时命中，任一 Agent 不得自行降级。

由此强制生效（不可裁量）：

1. Plan 之前必须先跑独立 `BEHAVIOR_DISCOVERY` Run 生成 inventory，生产方非 Planner，
   `produced_by_run_id` / `produced_by_context_id` 与 Plan Run 不同；
2. QA 拆 blind Charter 与 execution 两 Run；
3. Review 拆 `BLIND_BEHAVIOR_AUDIT`（Pass 1）与 `EVIDENCE_CHALLENGE`（Pass 2）两 Run；
4. 必须闭合最终 sink 与失败/撤权/陈旧状态；
5. Council lens 由 Planner 在 Plan 中提名 2–3 位，调度只校验名额、锚点互异、含反对者、
   rationale 是否引用具体 path/invariant。

## 2. 上游事实读回（不采信 Issue 正文，逐项重新核验）

读回时间 20260821-113700 ~ 113900 UTC。

| 事实 | Issue 正文所述 | 本次读回 | 一致性 |
|---|---|---|---|
| `iflytek/skillhub@main` head | `e8cab7389f5dc1084fdc1cdf70bafd314d0a3d50` | `e8cab7389f5dc1084fdc1cdf70bafd314d0a3d50` | 一致 |
| PR #735 head 分支 SHA | `e071afb4f0c1b124d484c9d1098b83924f0cfd1d` | `refs/heads/20260819-skillhub-731-confirm-publish-event-publication-r1` = `e071afb4f0c1b124d484c9d1098b83924f0cfd1d` | 一致（PR 仍停在 r1，断链事实成立） |
| 内部分支 r2 head | `607d8541d1f4479bf35ae54ce75afd68ff540ece` | `d0a10cb5e042dfe99e80467ada21fe1e37503965` | **不一致，r2 已前进 2 commit** |
| #738 已合入 main | 是 | `15ce199e1a951fed05139e4c984150ddfc45def3` `fix(publish): emit event on confirm publish (#738)`；`SkillReviewSubmitService.java:150` 已含 `publishEvent(new SkillPublishedEvent(` | 一致 |

r2 前进的 2 个 commit（`607d8541..d0a10cb5`）非 ledger 面为
`NotificationEventListener.java`、`SkillSubscriptionServiceTest.java`、
`NotificationEventListenerTest.java`，来自 HD-28 收尾轮。该事实不改变本任务的目标面，
但**任何引用 `607d8541` 的下游结论必须改用 `d0a10cb5` 重新读回**。

r2 相对 `upstream/main`：ahead 8 / behind 66（Issue 正文的 ahead 64 / behind 8 已过期）。

## 3. 工作面选择（编排决策，非语义裁决）

新建分支 `20260821-skillhub-735-subscription-authz-hardening-r3`，起点 = live upstream base
`e8cab738`，**不 rebase r2 的 66 个 commit**。理由（机械，可复核）：

1. R5 要求「rebase 到 main 最新」且「移除已由 #738 合入的 `confirmPublish` 改动」。
   从 live base 起面使这两项**由构造成立**，无需在 60 条 ledger 分录上做冲突 rebase；
2. #738 与 r2 改的是同一处 `SkillReviewSubmitService.java`，rebase 必产生冲突，
   而冲突的正确解法恰是「丢弃我方改动」——等价于不带入；
3. R3 要求把 `SubscriptionMetadataAccessPolicy` / `SubscriptionRecipientEligibility`
   两个新类收敛回 `VisibilityChecker`，该面本就是重写而非增量；
4. ledger append-only 不受影响：r2 的账本原样保留在该分支并已在 fork 上，
   本分支不复制、不改写、不继承其 verdict。

**r2 上可复用的材料（测试用例、i18n 文案、既有断言）由 Coder 按 Plan 择取移植，
移植与否属于实现判断，调度不预设。** 参考基线：`origin/20260819-skillhub-731-confirm-publish-event-r2`
@ `d0a10cb5`，其相对 upstream/main 的非 ledger 业务面 19 files / +1126 / -8。

## 4. 锚点路径核验（在 live base 上确认存在）

| 路径 | 状态 |
|---|---|
| `server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/skill/VisibilityChecker.java` | 存在，47 行 |
| `server/skillhub-domain/src/main/java/com/iflytek/skillhub/domain/social/SkillSubscriptionService.java` | 存在，65 行 |
| `server/skillhub-app/src/main/java/com/iflytek/skillhub/listener/NotificationEventListener.java` | 存在，289 行 |
| `server/skillhub-app/src/main/resources/messages.properties` | 存在，186 行 |
| `server/skillhub-app/src/main/resources/messages_zh.properties` | 存在，186 行 |

## 5. Gate bundle

已从 Agent skill 目录 `multica-multica-engineering-delivery/multica-gate/` 复制到
`/workspaces/HD-30/gates/`，`run_gate.sh` 可执行。
validator `scripts/validate_gate.py` sha256 前 16 位 `a7100d86232e21df`。
不从 `$HOME` 或 `/tmp` 运行。

## 6. 本轮不做的事（边界）

不 push 公开分支、不改动 PR #735、不转 Ready、不新建取代性 PR、不 force-push；
不写生产代码；不替 Planner 做语义判断（含 R1 `unsubscribe`/`isSubscribed` 语义、
R3 差异盘点结论、Council 人选）。

## 交接

- 状态：分支与账本骨架就绪，`HIGH_RISK` 已锁定
- 产物：分支 `20260821-skillhub-735-subscription-authz-hardening-r3`，base tree `c81497c`
- 下一步：`验证` 执行 `BEHAVIOR_DISCOVERY` Run，产出 `behavior-inventory.v1` 并冻结；
  **不得**由 Planner 生产
- 未解决：PR #735 的 state/draft/checks 未在本容器读回（本容器 `gh` 未认证，
  仅经 `git ls-remote` 确认 head 分支 SHA 未变）。该项不阻塞 Discovery，
  由后续具备认证的 Run 或主控会话补齐
- 需要决策：无（Plan Gate 与最终 Review Gate 的人类审批按 Project description 路由）
