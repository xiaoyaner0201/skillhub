---
ts: 20260821-133911
agent: leader
action: plan gate 机械校验 + 人工批准登记 + dispatch code/tdd
tree: c81497cf7eb98f0568b8b30c9af1e67797bca27a
verdict: PASS
---

Plan Gate 判定。本分录只登记机械校验结果、人工批准事实与 dispatch 编排，不含语义裁决。

## 1. 独立复跑（不采信 Planner receipt）

从 `afc7368ce5381ca9a1f352a6329b24fa72cc952e` 取出 artifact 与 inventory，用本容器
`/workspaces/HD-30/gates/` 的 validator 重跑 `--kind plan`：

- 结果 `PASS`，`warnings=[]`，`errors=[]`
- 本分录 receipt：`leader/20260821-133911-plan-gate-receipt-leader.json`
- 与 Planner 的 `planner/20260821-125849-plan-gate-receipt-r1.json` 逐字段一致；两份文件的
  字节 SHA-256 不同（`550b6359…` vs `22d78a6d…`）仅因 JSON 序列化格式不同（单行 vs
  pretty-print），`python3 -m json.tool` 规范化后 `diff` 为空。

## 2. 绑定核对

| 绑定项 | 值 | 核对方式 |
|---|---|---|
| plan artifact | `39510ad88cff8cb34f7efc584eb9818a47e6f1cb032c0bf2c11caf3c018d2236` | 对 tree 内 blob 重算 |
| 人类计划 | `4465119ef0f150a578893cbf70fa7f4c66c81e0d13696620a8757de3334f35de` | 同上 |
| inventory | `efd911ad203add676cf925c8e20e1cb40251415d1a90600b2ad3de2f48131eae` | 与 Discovery 冻结值同一份 |
| subject base tree | `c81497cf7eb98f0568b8b30c9af1e67797bca27a` | `git rev-parse e8cab738^{tree}` |
| validator | `a7100d86232e21df4f032d07ecbc7e055f2353020be0858230db131a47ca4892` | `sha256sum scripts/validate_gate.py` |
| Plan Run provenance | run `553ad2f2-5673-4fae-ad25-fbdf193f2ad3` / context `01a0245b-3485-7d62-8601-0e432a47f693` | 异于 Discovery 的 `8e9589ec…` / `01a02421…` |

## 3. validator 之外的机械项

- 闭包：15 candidate / 0 excluded / 0 unresolved；无 authz 类以 `non-goal` 排除。
- write_set 18 条逐条比对 base tree：13 条 EXISTS、5 条 NEW；无 `SkillReviewSubmitService`、
  `confirmPublish`、migration、`.github/workflows`。R5 由构造成立。
- 场景覆盖：`subscription-create` / `published-subscriber-fanout` / `yanked-subscriber-fanout`
  三条路径各覆盖 private / namespace-only / hidden / removed-member / disabled-user 五态，
  共 15 组具名 `state_id`（R4 要求十组）。28 个 probe 中 14 个 `expected_before=FAIL`。
- Council：3 名额，锚点互异（Bruce Schneier-style / Leslie Lamport-style / Kent Beck-style），
  含 1 个 `dissent_role=true`，rationale 引用的 path/invariant id 在 artifact 内真实存在。
- 边界扫描：全文无「维护者已批准 / 已获批」类表述；A/B/C 存量语义写为待上游裁决。
- 非 ledger 业务面 diff 相对 `e8cab738` 为空。

## 4. 人工批准（Project description 授权路由）

Plan Gate 的批准决策按 Project description 授权给千乘妍独立审批。于 20260821-132345 以
`in_review` + 真实 mention 交出，20260821-133x 收到 **APPROVE**（Issue HD-30 评论）。

审批人独立核验并采信的三点：`VisibilityChecker` production 调用点经 GitHub code search
独立枚举命中 6 个 production 类（R3 按 8 处算成立）；PR #735 现状 `OPEN` / `isDraft=true` /
head `e071afb4f0c1b124d484c9d1098b83924f0cfd1d` / `mergeStateStatus=DIRTY`，draft 边界未破；
业务面仍为空、三项绑定一致。

三个实质取舍逐条批准：动上游共享类（新增 overload、8 caller 不迁移只回归）；
`unsubscribe` / `isSubscribed` 冻结 A 案；存量行为破坏面留到 PR 正文披露、本链不做数据清理。

审批附加的 Review Gate 验收约束（不构成 RETURN，登记为后续 Gate 判据）：

1. 两个 `UNVERIFIED` candidate 不得以「测试没覆盖到就是没有」结案，须给实测 probe 或显式
   声明为残留盲区并写进 PR 正文；
2. R2 的「不误伤」须有对可读订阅者集合过滤前后逐元素一致的正向证据，不得只测拒绝路径；
3. RED 证据须真实先红后绿，不接受先实现后补测试。
4. `SubscriberAccessResolver` 逐行禁止独立判定分支这条，须在 Review 阶段被逐行证伪，
   不接受「设计上没有」的自述。

## 5. dispatch

派 Code/TDD 给 Coder（`fd2d1252-2974-4998-8b7e-128c7c7c2c36`），工作面
`20260821-skillhub-735-subscription-authz-hardening-r3` @ `afc7368c`，冻结计划
`planner/20260821-125849-plan-r1.md`。Issue 状态从 `in_review` 回到 `in_progress`。

Publication 边界不变：本链只产出内部分支 exact tree。
